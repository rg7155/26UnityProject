# 탕탕특공대 스타일 포트폴리오 기획서
**프로젝트명:** Rewind Survivors (가칭)
**엔진:** Unity 2D (LTS 2022.3 권장)
**개발 기간:** 6개월 (퇴근 후 작업 기준, 주 3~4회)
**목적:** 클라이언트 게임플레이 프로그래머 포트폴리오
**아트:** 도형 기반 (원, 삼각형, 사각형) — 아트 리소스 부담 없음

### 기술 스택 결정사항
| 항목 | 선택 | 이유 |
|------|------|------|
| 렌더 파이프라인 | **Built-in** | DrawMeshInstanced 별도 작업 없이 바로 사용 가능, URP 삽질 방지 |
| Input System | **구 Input Manager** | WASD + Shift 뿐이라 New Input System 복잡함 불필요, 핵심 시스템에 집중 |

---

## 1. 게임 개요

### 한 줄 설명
> 수백 마리의 적을 자동 공격으로 생존하면서, 시간 되돌리기로 위기를 극복하는 탑다운 2D 서바이버

### 레퍼런스
- Vampire Survivors (핵심 게임루프)
- Braid (시간 되돌리기 레퍼런스)

### 타겟 플랫폼
- PC (Editor 실행 기준) → Windows / WebGL 빌드

### 핵심 기술 어필
1. Spatial Hashing 기반 충돌 최적화
2. Object Pooling at Scale (타입별 풀 + Warm-up)
3. GPU Instancing (DrawMeshInstanced)
4. Time Rewind System (Circular Buffer + State Snapshot)

---

## 2. 핵심 게임플레이

### 기본 규칙
- 플레이어(흰 원)가 WASD로 이동
- 적(삼각형, 사각형 등)이 웨이브마다 대량 스폰, 플레이어를 향해 이동
- 무기는 자동 공격 (Vampire Survivors 방식)
- 일정 시간 생존 or 적 처치 목표 달성 시 스테이지 클리어
- 레벨업 시 업그레이드 3개 중 1개 선택

### 적 종류 (MVP 기준)
| 타입 | 도형 | 색상 | 특징 |
|------|------|------|------|
| 기본적 | 삼각형 | 빨강 | 단순 추적 |
| 탱커 | 큰 사각형 | 보라 | 느리고 HP 많음 |
| 스피더 | 작은 삼각형 | 노랑 | 빠름, HP 낮음 |
| 폭발형 | 마름모 | 주황 | 사망 시 범위 피해 |

### 무기 종류 (MVP 기준)
- 기본 발사체 (직선)
- 회전 오브 (근거리 회전)
- 범위 폭발 (일정 주기)

### 레벨업 / 업그레이드 시스템
- 적 처치 시 경험치 드롭
- 경험치 누적 → 레벨업 → 게임 일시정지 후 업그레이드 3개 중 1개 선택
- 업그레이드는 중복 선택 가능 (누적 적용)

| 업그레이드 | 효과 | 대상 필드 |
|-----------|------|---------|
| 이동속도 증가 | Speed × 1.2 | PlayerController._speed |
| 발사속도 증가 | FireRate × 1.3 | PlayerWeapon._fireRate |
| 데미지 증가 | Damage × 1.25 | PlayerWeapon._damage |
| 최대 HP 증가 | MaxHp + 20 (즉시 회복 포함) | PlayerController._maxHp |
| 사거리 증가 | Range × 1.2 | PlayerWeapon._range |

### 시간 되돌리기 시스템
| 모드 | 발동 조건 | 되감기 시간 | 쿨타임/제한 |
|------|---------|-----------|-----------|
| 능동 Rewind | Shift 키 (능동 사용) | 최대 5초 | 30초 쿨타임 |
| 사망 Auto-Rewind | HP 0 도달 시 자동 | 고정 5초 | 스테이지당 1회 |

---

## 3. 기술 어필 포인트 (핵심)

---

### ① Spatial Hashing — 충돌 감지 최적화

**왜 필요한가:**
- 적 500마리 기준, 단순 충돌 검사는 O(n²) = 250,000번/프레임
- Spatial Hashing 적용 시 O(n)에 근접

**설계:**
```
World Space를 고정 Cell 크기(예: 2.0f)로 분할
각 적은 자신이 속한 Cell에 등록

충돌 검사 시:
  → 대상 위치의 Cell 좌표 계산
  → 인접 9개 Cell 내 오브젝트만 검사
  → 전체 탐색 없이 후보군 대폭 축소

Cell Key = hash(cellX, cellY)
         = cellX * 73856093 ^ cellY * 19349663
```

**Cell 크기 트레이드오프:**
- Cell 너무 작음 → 등록/해제 오버헤드 증가
- Cell 너무 큼 → 후보군이 너무 많아 이점 감소
- 적의 평균 충돌 반경 × 2 를 기준으로 결정

**성능 측정 계획:**
- 500 / 1000 / 2000 적 기준 FPS 측정
- Unity Profiler로 `Physics2D`와 커스텀 SpatialHash 비교 캡처

**면접 멘트:**
> "단순 Physics2D.OverlapCircle을 전체 적에게 돌리면 적이 늘수록 프레임이 급격히 떨어집니다. Spatial Hashing으로 충돌 후보군을 인접 셀로 한정해서 적 1000마리에서도 안정적인 프레임을 유지했습니다."

---

### ② Object Pooling at Scale

**왜 일반 풀링과 다른가:**
- 단순 풀링은 "미리 만들어둔다"가 전부
- 여기서 어필할 것은 **설계 결정과 그 이유**

**설계:**
```csharp
// 타입별 풀 딕셔너리 (Generic Pool)
Dictionary<PoolKey, Queue<GameObject>> _pools;

// 풀 초과 시 전략: Grow (동적 확장)
// 이유: 게임 극초반 스폰 버스트에서 Reject는 UX 파괴

// Warm-up 타이밍: 스테이지 로드 시점 (플레이 중 GC 스파이크 방지)
// Warm-up 수량: 최대 예상 동시 오브젝트 수의 80%

// 오브젝트 반납 시 위치/상태 초기화 책임은 Pool이 아닌 오브젝트 본인
// 이유: Pool이 각 타입의 초기화 방식을 알 필요 없음 (단일 책임 원칙)
```

**풀링 대상:**
- 적 GameObject (타입별)
- 발사체 GameObject
- 히트 이펙트 (파티클 없이 도형으로 처리)
- 데미지 텍스트

**성능 측정 계획:**
- Unity Profiler Memory 탭 GC Alloc 0 검증
- 풀링 전/후 GC.Collect 호출 횟수 비교

---

### ③ GPU Instancing — 렌더링 최적화

**왜 필요한가:**
- 적 1000마리가 각자 Draw Call을 날리면 CPU 병목
- 같은 메시(삼각형)를 쓰는 적은 한 번의 Draw Call로 처리 가능

**설계:**
```csharp
// Graphics.DrawMeshInstanced 사용
// 최대 1023개 인스턴스를 단일 Draw Call로 처리

// 적 타입별로 별도 인스턴스 배치 관리
// 매 프레임 Matrix4x4[] 갱신 후 DrawMeshInstanced 호출

// 색상 변화(피격 시 빨개짐 등)는 MaterialPropertyBlock으로 인스턴스별 처리
MaterialPropertyBlock _mpb;
_mpb.SetColor("_Color", hitColor);
```

**주의할 설계 결정:**
- DrawMeshInstanced는 Unity의 일반 렌더러 파이프라인 밖
- 즉, 이 방식을 쓰면 적 GameObject에서 MeshRenderer를 제거하고 직접 관리해야 함
- 이게 오히려 어필: "렌더링 파이프라인을 이해하고 의도적으로 우회한 것"

**성능 측정 계획:**
- Frame Debugger로 Draw Call 수 before/after 비교
- 1000마리 기준 DrawMeshInstanced vs 일반 MeshRenderer FPS 비교

---

### ④ Time Rewind System — 핵심 차별화 기능

**설계 방식 선택: State Snapshot + Circular Buffer**

**Command Pattern이란?**
모든 상태 변화를 "명령" 단위로 기록, 역재생 시 명령을 반대로 실행하는 방식.
- 예: `Move(+1, 0)` → `Attack()` 기록 → 역재생 시 `Undo_Attack()` → `Move(-1, 0)`
- 조건: 게임이 **결정론적**이어야 함 (같은 입력 → 항상 같은 결과)
- 실제 사용 사례: 스타크래프트 1/2 리플레이, 워크래프트3 리플레이, 대전격투 입력 판정
- 장점: 메모리 매우 적게 사용 (입력만 저장 — 스타크 1시간 리플레이가 수백KB)
- 단점: 비결정론적 요소(물리, AI 랜덤) 있으면 역재생 깨짐, Undo 로직 모든 명령마다 구현 필요

**State Snapshot이란?**
특정 시점의 게임 상태 전체를 복사해서 저장, 복원 시 그냥 덮어씌우는 방식.
- 예: `{플레이어:(3,2), HP:80, 적1:(5,3)...}` 저장 → 복원 시 그대로 적용
- 조건: 저장할 상태가 명확히 정의되어야 함
- 실제 사용 사례: 프린스 오브 페르시아(2008), Braid, 대부분의 세이브/로드 시스템
- 장점: AI가 비결정론적이어도 무관, 중간 지점으로 바로 점프 가능
- 단점: 메모리 많이 사용, 스냅샷 저장 자체가 CPU 비용

**혼합 방식 (가장 정교한 방법):**
언리얼 엔진 리플레이, 격겜 롤백 넷코드에서 사용.
```
키프레임(State Snapshot) ─────────────────────
                          ↑                  ↑
         Command  Command Command    Command Command
```
일정 간격으로 전체 상태 저장(키프레임), 그 사이는 입력만 기록.
복원 시: 가장 가까운 키프레임 → 이후 입력 재생. 메모리와 정확도를 동시에 잡는 방식.

Command Pattern vs State Snapshot 트레이드오프:
| | Command Pattern | State Snapshot |
|--|--|--|
| 개념 | 모든 입력/명령을 기록해 역재생 | 매 tick마다 게임 상태 전체를 저장 |
| 적합한 경우 | 결정론적, 입력 기반 게임 | 비결정론적, AI 이동이 있는 게임 |
| 실제 사례 | 스타크래프트 리플레이, 격겜 입력 | 프오페, Braid, 세이브/로드 |
| 메모리 | 매우 적음 | 오브젝트 수에 비례 |
| 이 프로젝트 | 부적합 (적 AI가 비결정론적) | **채택** |

**메모리 설계 (면접에서 숫자로 말할 수 있어야 함):**
```
Snapshot 저장 주기: 0.05초 (20fps 기록)
되감기 최대 시간: 5초
필요한 Snapshot 수: 5 / 0.05 = 100개

엔티티 1개당 저장 데이터:
  position  : Vector2 = 8 bytes
  hp        : float   = 4 bytes
  isAlive   : bool    = 1 byte
  entityId  : int     = 4 bytes
  = 약 17 bytes

적 최대 1000마리 기준:
  1000 × 17 bytes = 17KB / snapshot
  100 snapshots = 1.7MB 총 메모리 예산

→ 허용 가능한 범위. 부족하면 주기를 0.1초로 낮춰 0.85MB로 절감 가능
```

**Circular Buffer 구조:**
```csharp
public class CircularSnapshotBuffer
{
    private GameSnapshot[] _buffer;  // 고정 크기 배열
    private int _head;               // 가장 오래된 snapshot 인덱스
    private int _tail;               // 가장 최신 snapshot 인덱스
    private int _count;

    // 새 snapshot 추가: tail을 한 칸 전진
    // 꽉 찼을 때: head도 전진 (가장 오래된 것 자동 덮어씀)
    // O(1) 삽입, O(1) 접근
}
```

**되감기 중 스폰/소멸 오브젝트 처리 (핵심 엣지케이스):**
```
문제: 되감는 동안 "되감기 도중 죽은 적"을 다시 살려야 함
      "되감기 도중 스폰된 적"은 다시 Pool로 반납해야 함

해결: Snapshot에 "이 시점에 살아있는 EntityId 목록" 저장
      되감기 시 현재 살아있는 목록과 Snapshot 목록을 diff
      → 목록에 없는데 살아있으면: Pool 반납 (스폰된 것 제거)
      → 목록에 있는데 죽어있으면: Pool에서 꺼내 부활 (소멸된 것 복원)
```

**되감기 보간 (Smooth Rewind):**
```
단순 Snapshot 복원 → 오브젝트가 순간이동처럼 보임
Snapshot 사이 구간은 Lerp로 보간 → 자연스럽게 뒤로 감기는 연출
```

**면접 멘트:**
> "Command Pattern도 검토했는데, 적 AI가 비결정론적이라 역재생이 불가능해서 State Snapshot 방식을 선택했습니다. 메모리 예산을 계산하고 Circular Buffer로 고정 메모리 사용을 보장했으며, 되감기 중 스폰/소멸된 오브젝트를 EntityId diff로 처리한 게 구현에서 가장 까다로운 부분이었습니다."

---

### 적 되돌리기 구현 계획 (EntityId 시스템)

**왜 어려운가 — Object Pool 재사용 문제:**
```
시나리오:
1. 적A 스폰 (GameObject #1 사용)
2. 적A 사망 → Pool 반납 (GameObject #1 비활성)
3. 적B 스폰 → Pool에서 같은 GameObject #1 꺼냄
4. 이제 적A와 적B가 물리적으로 같은 오브젝트

되감기 시 문제:
"5초 전 스냅샷에 적A가 살아있었다"
→ GameObject #1을 활성화해야 하는데
→ 현재 GameObject #1은 적B로 사용 중
→ 충돌!
```

**해결책 — EntityId 시스템:**
```csharp
// EnemyBase에 고유 ID 부여
static int _nextEntityId = 0;
public int EntityId { get; private set; }

// Init() 호출 시마다 새 ID 발급 (Pool에서 꺼낼 때마다)
public void Init(...)
{
    EntityId = _nextEntityId++;
    ...
}

// 스냅샷에 ID 저장
struct EnemySnapshot
{
    public int entityId;   // 추가
    public int prefabId;   // 어떤 프리팹인지
    ...
}
```

**Rewind 시 diff 처리:**
```
현재 살아있는 EntityId 목록: [3, 7, 12, 15]
5초 전 스냅샷의 EntityId 목록: [1, 3, 5, 7]

→ 현재에만 있는 [12, 15]: 되감기 도중 스폰된 것 → Pool 반납
→ 스냅샷에만 있는 [1, 5]: 되감기 도중 사망한 것 → Pool에서 꺼내 부활
→ 공통 [3, 7]: 위치/HP만 복원
```

**추가로 필요한 구현:**
| 항목 | 내용 | 난이도 |
|------|------|--------|
| EntityId 발급 | Init마다 static 카운터 증가 | 쉬움 |
| 활성 적 레지스트리 | `Dictionary<int, EnemyBase>` 전역 관리 | 보통 |
| 스냅샷 diff | 현재 목록 vs 스냅샷 목록 비교 | 보통 |
| Pool에서 부활 | 특정 EntityId 적을 Pool에서 꺼내 복원 | 어려움 |
| 프리팹 매핑 | EntityId → 어떤 프리팹인지 알아야 Pool에서 꺼낼 수 있음 | 보통 |

**"어렵다"는 게 얼마나인가:**
- 코드 줄 수 자체는 많지 않음 (약 100~150줄 추가)
- 어려운 이유는 **버그 시나리오가 많음**
  - 되감기 도중 새 적이 스폰되면? → Rewinding 상태로 차단
  - 부활시킨 적의 _target(플레이어 참조)이 null이면? → 재설정 필요
  - 같은 프레임에 여러 적이 동시에 죽으면? → 순서 보장 필요
  - Pool이 비어서 부활할 오브젝트가 없으면? → 새로 Instantiate
- 즉, 로직 자체보다 **엣지케이스 처리**가 대부분의 작업량

---

### 적 되돌리기 구현 설계 (사전 분석)

**현재 코드의 문제점 (구현 전 파악):**

1. `CaptureEnemies`에서 `FindObjectsOfType<EnemyBase>(true)` 사용
   - 매 0.05초마다 씬 전체 스캔 → GPU Instancing, Spatial Hashing으로 최적화한 것과 모순
   - 해결: EnemyBase 내부 static 레지스트리로 대체

2. ApplyEnemies의 Pool 재사용 버그
   ```csharp
   // 현재 코드 — Pool에서 꺼낸 obj를 사용하지 않고 s.enemy를 직접 활성화
   GameObject obj = Managers.Object.Get(s.prefab); // 사용 안 됨
   s.enemy.gameObject.SetActive(true);              // s.enemy가 재사용된 오브젝트면 오동작
   ```
   - Pool 재사용 시 `s.enemy`가 가리키는 컴포넌트가 이미 다른 적으로 사용 중일 수 있음
   - 해결: EntityId로 논리적 동일성 확인 후 케이스 분기

3. EntityId 없음 → Pool 재사용 시 논리적 동일성 구분 불가

**3케이스 diff 설계:**
```
[케이스 A] 스냅샷에 있고 현재도 활성
  → 위치/HP만 복원 (가장 단순)

[케이스 B] 스냅샷에 있지만 현재 비활성 (되감기 도중 사망)
  → s.enemy.EntityId == s.entityId ?
       YES: 같은 논리적 적 → 그대로 재활성화 + 복원
       NO:  Pool 재사용됨 → Pool에서 같은 프리팹 꺼내 ForceRestore

[케이스 C] 현재 활성이지만 스냅샷에 없음 (되감기 도중 스폰)
  → Pool 반납 (SpatialHashGrid, EnemyInstanceRenderer 해제 포함)
```

**ForceRestore 설계:**
- `Init()`을 거치지 않고 상태만 강제 주입
- `_target`, `_originPrefab`, `EntityId` 모두 스냅샷 기준으로 설정
- registry에 재등록

**예상 버그 & 대응:**
| 시나리오 | 문제 | 대응 |
|----------|------|------|
| 되감기 중 스폰 타이밍 | WaveManager가 적 스폰 | `Rewinding` 상태로 차단 (이미 구현) |
| Pool이 비어서 부활 불가 | `Get()`이 새로 Instantiate | Pool 동적 확장으로 자동 해결 |
| 부활한 적의 `_target` null | 플레이어 참조 없음 | ForceRestore에서 playerTarget 전달 |
| Pool 재사용이 5초 윈도우 안에서 발생 | EntityId 불일치 | 케이스 B에서 감지 및 처리 |
| 되감기 완료 후 registry 불일치 | 활성 목록 오염 | ForceRestore 시 registry 재등록 |

**구현 단계:**
1. EnemyBase — EntityId + static _registry
2. EnemySnapshot — entityId 필드 추가, CaptureEnemies 레지스트리로 교체
3. RewindManager.ApplyEnemies — 3케이스 diff 로직
4. EnemyBase.ForceRestore — Pool 재사용 케이스 처리
5. 테스트 — 적 사망 직후 되감기, Pool 재사용 상황

---

## 4. 개발 범위

### MVP 포함
- [x] 플레이어 이동 (WASD)
- [x] 적 3종 + 스폰 웨이브 시스템
- [x] 자동 공격 무기 2종
- [x] 레벨업 + 업그레이드 선택 UI
- [x] Spatial Hashing 충돌 시스템
- [x] Object Pooling (타입별 Generic Pool)
- [x] GPU Instancing (적 렌더링)
- [x] Time Rewind — 능동 스킬 (5초, 쿨타임 30초)
- [x] Time Rewind — 사망 Auto-Rewind (스테이지당 1회)
- [x] 기본 UI (HP, 타이머, 레벨, Rewind 쿨타임 게이지)
- [x] 스테이지 1개 (생존 10분 목표)
- [x] 성능 측정 씬 (적 수 조절하며 FPS 측정용)

### 의도적 제외
- [ ] 서버/랭킹
- [ ] 스토리/컷씬
- [ ] 복잡한 보스 패턴
- [ ] 사운드 (시간 남으면 추가)
- [ ] Unity Job System / Burst (5개월차 여유 있으면 추가 도전)

---

## 5. 개발 타임라인 (6개월, 주 3~4회 퇴근 후)

### 전체 요약
| 월 | 목표 | 완료 기준 |
|----|------|---------|
| **1개월** | 기초 루프 | "돌아는 간다" 수준 |
| **2개월** | 게임 시스템 완성 | 10분 플레이 가능 |
| **3개월** | 최적화 1단계 | 1000마리 60fps |
| **4개월** | Time Rewind | Rewind 버그 없이 동작 |
| **5개월** | 폴리싱 | 지인한테 보여줘도 부끄럽지 않은 수준 |
| **6개월** | 문서화 | GitHub에 올릴 수 있는 상태 |

> **주의:** 3개월차(최적화)와 4개월차(Rewind)가 가장 어렵다. 각 월에 버퍼 1주 여유를 두고 진행할 것.

---

### 1개월차 — Unity 재적응 + 기초 루프
| 주차 | 목표 |
|------|------|
| 1주 | Unity 프로젝트 세팅, Input System, 플레이어 이동 |
| 2주 | 적 기본 스폰 (단일 타입), 플레이어 추적 AI |
| 3주 | 기본 충돌 (Physics2D 우선), 피해/HP 시스템 |
| 4주 | 기본 자동 발사체, 게임 루프 (생존/사망) |

> **이 시점 목표:** "돌아는 간다" 수준. 최적화 없어도 됨

---

### 2개월차 — 게임 시스템 완성
| 주차 | 목표 |
|------|------|
| 1주 | 웨이브 시스템 (시간에 따라 적 수/종류 증가) |
| 2주 | 레벨업 + 업그레이드 선택 UI |
| 3주 | 무기 2종 추가, 적 3종 추가 |
| 4주 | 기본 UI 완성, 스테이지 완성도 높이기 |

> **이 시점 목표:** 게임으로서 플레이 가능한 상태

---

### 3개월차 — 최적화 1단계
| 주차 | 목표 |
|------|------|
| 1주 | Object Pool 설계 및 적용 (타입별 Generic Pool) |
| 2주 | Spatial Hashing 구현 + Physics2D 제거 |
| 3주 | GPU Instancing 적용 (DrawMeshInstanced) |
| 4주 | **성능 측정**: 500 / 1000 / 2000 적 기준 FPS + Draw Call 측정, Profiler 캡처 |

> **이 시점 목표:** 1000마리에서 60fps 목표, 수치 기록

---

### 4개월차 — Time Rewind 시스템
| 주차 | 목표 |
|------|------|
| 1주 | Circular Buffer 구현, Snapshot 설계 |
| 2주 | 능동 Rewind 구현 (키 누르면 5초 되감기) |
| 3주 | 사망 Auto-Rewind 구현 |
| 4주 | 엣지케이스 처리 (스폰/소멸 오브젝트 diff, 보간) |

> **이 시점 목표:** Rewind가 버그 없이 동작, 엣지케이스 정리

---

### 5개월차 — 폴리싱 + 선택적 최적화
| 주차 | 목표 |
|------|------|
| 1주 | 버그 수정, 게임 밸런스 조정 |
| 2주 | 도형 기반 비주얼 다듬기 (색상, 크기로 적 구분 명확히) |
| 3주 | Rewind 연출 (화면 색 반전 or 간단한 포스트프로세싱) |
| 4주 | Unity Job System 도전 (여유 있으면) — 적 이동 병렬화 |

---

### 6개월차 — 포트폴리오 문서화 (가장 중요)
| 주차 | 목표 |
|------|------|
| 1주 | GitHub README 작성 (아키텍처 다이어그램) |
| 2주 | 성능 측정 결과 정리 (before/after 표, Profiler 캡처) |
| 3주 | 플레이 영상 GIF, Rewind 시연 영상 녹화 |
| 4주 | 면접 Q&A 준비, 코드 정리 및 주석 |

---

## 6. 폴더 구조

```
Assets/
├── Scripts/
│   ├── Player/
│   │   ├── PlayerController.cs       ← 이동 입력 처리
│   │   └── PlayerStats.cs
│   ├── Enemy/
│   │   ├── EnemyBase.cs              ← 적 공통 인터페이스
│   │   ├── EnemyMover.cs             ← 이동 로직 (Spatial Hash 등록 포함)
│   │   └── EnemyTypes/               ← 적 타입별 구현
│   ├── Weapon/
│   │   ├── WeaponBase.cs
│   │   └── Weapons/
│   ├── Combat/
│   │   └── SpatialHashGrid.cs        ← 핵심: 공간 분할 충돌 시스템
│   ├── Rendering/
│   │   └── InstancedRenderer.cs      ← GPU Instancing 관리
│   ├── Pool/
│   │   ├── ObjectPool.cs             ← Generic Pool 구현
│   │   └── PoolManager.cs            ← 타입별 풀 딕셔너리 관리
│   ├── Rewind/
│   │   ├── RewindManager.cs          ← 되감기 트리거 및 제어
│   │   ├── CircularSnapshotBuffer.cs ← Circular Buffer 구현
│   │   ├── GameSnapshot.cs           ← 한 시점의 게임 상태
│   │   └── IRewindable.cs            ← 되감기 가능한 오브젝트 인터페이스
│   ├── Wave/
│   │   └── WaveManager.cs            ← 웨이브/스폰 제어
│   └── UI/
│       └── GameUIController.cs
├── Data/
│   └── WaveConfigs/                  ← ScriptableObject 기반 웨이브 설정
└── Prefabs/
    ├── Enemies/
    ├── Weapons/
    └── Effects/
```

---

## 7. GitHub README 필수 항목

1. **성능 비교표** — 적 수별 FPS, Spatial Hashing before/after, Draw Call 수
2. **Rewind 시스템 다이어그램** — Circular Buffer 구조, Snapshot diff 흐름
3. **메모리 예산 계산 근거** — "왜 0.05초 간격인가"를 숫자로 설명
4. **Rewind 시연 GIF** — 능동 되감기 + 사망 Auto-Rewind 각 1개씩
5. **아키텍처 다이어그램** — 주요 시스템 간 의존 관계

---

## 8. 면접 예상 Q&A

**Q. Spatial Hashing을 쓴 이유가 뭔가요?**
> 적이 1000마리일 때 Unity Physics2D로 충돌을 검사하면 매 프레임 O(n²) 연산이 발생합니다. Spatial Hashing으로 인접 셀만 검사하도록 해서 적 수에 비례하는 O(n) 수준으로 낮췄고, 실제로 1000마리 기준 FPS가 [before] → [after]로 개선됐습니다.

**Q. Command Pattern 대신 State Snapshot을 선택한 이유는?**
> Command Pattern은 모든 상태 변화가 명령으로 표현되는 결정론적 게임에 적합합니다. 이 게임은 적 AI가 비결정론적이라 역재생이 불가능해서 State Snapshot 방식을 선택했습니다.

**Q. Circular Buffer를 쓴 이유가 뭔가요?**
> 5초치 Snapshot을 고정 크기 배열에 저장하고 오래된 것부터 덮어씁니다. 동적 할당 없이 O(1) 삽입이 가능하고, 메모리를 예산 내에서 관리할 수 있습니다. 적 1000마리 기준 5초 = 약 1.7MB로 계산하고 설계했습니다.

**Q. 되감기 중 죽은 적은 어떻게 처리했나요?**
> 각 Snapshot에 해당 시점의 살아있는 EntityId 목록을 저장합니다. 되감기 적용 시 현재 살아있는 목록과 Snapshot의 목록을 diff해서, 복원해야 할 적은 Pool에서 꺼내 부활시키고, 제거해야 할 적은 Pool로 반납합니다.

**Q. GPU Instancing을 직접 구현한 이유는?**
> Unity의 일반 MeshRenderer는 동일 메시라도 오브젝트별로 Draw Call이 발생합니다. DrawMeshInstanced를 직접 호출해서 같은 타입의 적 전체를 단일 Draw Call로 처리했고, Frame Debugger로 Draw Call이 [before] → [after]로 줄어든 것을 확인했습니다.

---

## 9. 메타 상점 & 골드 경제

### 컨셉
한 판이 끝나도 골드가 남는다. 로비(타이틀)에서 "다음 판의 나"를 조립한다. **①② 계열은 파워를 주고, ③(Rewind 강화)은 이 게임만의 정체성을 준다** — 둘 다 사야 클리어되게 설계해 골드 소비처가 마르지 않는다.

### 화폐 / 획득
- 적 처치 시 **골드** 획득, 런 간 **영구 누적**(GameData JSON 확장). `_goldReward`를 exp와 **별도 필드**로 두어 "판 안 성장(exp)"과 "판 간 성장(gold)"을 독립 조절.
- 적 타입별: 기본 **1** / Speeder **2** / Tanker **3**
- 평균 한 판 ≈ 300~500골드 (현재 스폰율 기준 추정 — 실측 후 전 가격 동일 배율 재조정)
- (2단계) 생존 보너스: `생존초 × 0.5` 골드 → 되감기 잘 쓰는 오래 버티는 플레이 보상

### 계열 ① 일반 능력치 강화 (각 5티어, 티어마다 가격 2~2.5배 급증)
| 항목 | 효과/티어 | 상한 |
|------|-----------|------|
| 시작 HP | +15 | T5 (100→175) |
| 기본 데미지 | +8% | T5 (+40%) |
| 이동 속도 | +4% | **T3 상한 (+12%)** — 무한 kiting 방지 |
| 행운 (3택 품질) | 3택 rarity/무기 확률↑ | T3, 고가 |
| exp 획득량 | +6% | T4 |
| 시작 골드 | 판 시작 +25 지참 | T2 |

### 계열 ② 무기 해금 (3택 풀에 추가 — 랜덤성 유지, 일회성 구매)
| 무기 | 시작 상태 | 해금 비용 |
|------|-----------|-----------|
| 기본 투사체 | 시작 장착 | — |
| RapidShot | 해금됨 | 무료 |
| Bomb | 잠김 | 400 |
| Orbit | 잠김 | 800 |
| Lightning | 잠김 | 1,500 |
> 해금 = 3택 등장 풀 진입(들고 시작 아님). 무기 강화는 판 안 3택이 담당 → 코어 재미 침범 없음. 시작 풀을 좁게 둬 해금 욕구 유발.

### 계열 ③ Rewind 강화 (시그니처 — 상한 명확)
| 항목 | 효과 | 상한/이유 |
|------|------|-----------|
| 자동부활 횟수 | +1회 | 최대 총 4회 (무한 부활 금지) |
| 되감기 길이 | +1초 (5→7) | 최대 7초 (버퍼/손맛) |
| 되감기 쿨타임 | −5초 (30→20) | 최소 20초 (상시 회피 방지) |
| **되감기 중 처치 골드 2배** | 리플레이 중 처치 골드 ×2 | **능동(Shift) 되감기 한정** |
| 부활 무적 연장 | 1s→2.5s | 재사망 방지 |

**크로스룰 하이라이트:** "되감기 중 골드 2배"는 **능동 되감기에만** 적용(자동부활 제외 — "일부러 죽어 파밍" 방지). 위기를 만들고→되감고→파밍하는 새 플레이 패턴 → 되감기가 "실수 취소"에서 "적극 운용 자원"으로 격상.

### 밸런스 / 파워 크립 통제
- 모든 항목 **상한 존재**, 티어별 비용 급증 → 곡선 평탄화
- **①=파워, ③=안전** — 한쪽만 극단으로 밀면 못 깬다(둘 다 필요)
- 전체 해금 ≈ 40~60판(장기 목표). (2단계) 메타 강화에 맞춰 무한 웨이브 적 스케일 상향

### 스탯 가시성 (선결 조건)
- 상점 구매 버튼에 **before→after 수치**(예 `100 → 115`) — 1단계 필수
- **별도 스탯창(추후)**: 생존/공격/기동/성장/되감기/해금 항목 표시
- ⚠️ **구현 선결 조건:** 데미지/공속/사거리 배율이 `WeaponManager`에 흩어져 집계 노출점 없음 → **"현재 능력치를 한곳에서 읽는 접근점"이 상점·스탯창의 전제**

### 단계적 구현 (각 독립 완결)
1. **MVP:** 골드(GameData JSON) + 처치 누적 + 결과 화면 표시 + 타이틀 상점 패널 + ① 2~3항목 + Bomb 해금 + before→after
2. **3계열 완성:** ① 전 항목 + ② 전 무기 해금 + ③ Rewind 라인 + 생존 보너스
3. **폴리싱:** 골드 2배 크로스룰 + 스탯창 + 무한 웨이브 적 스케일 상향
