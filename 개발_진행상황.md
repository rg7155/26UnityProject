# Rewind Survivors — 개발 진행상황

## 1개월차 — Unity 재적응 + 기초 루프

### 1주차
| 항목 | 상태 | 비고 |
|------|------|------|
| 프로젝트 세팅 | ✅ | URP / New Input System (Unity 6 기본값) |
| 매니저 프레임워크 | ✅ | Managers, ResourceManager, SoundManager, GameManagerEx, DataManager, ObjectManager, UIManager, SceneManager |
| Define / Utils | ✅ | GameState, CreatureState, SceneType, Sound enum |
| 플레이어 이동 (WASD) | ✅ | New Input System Polling 방식 (Keyboard.current) |
| 카메라 추적 | ✅ | LateUpdate Lerp 방식 |
| 적 기본 구조 | ✅ | EnemyBase (스탯/피해), EnemyMover (추적 이동) |
| 적 스폰 | ✅ | EnemySpawner — 플레이어 자동 참조, 랜덤 위치 스폰 |
| 그리드 배경 | ✅ | URP RenderPipelineManager 방식 (OnPostRender 대체). 5개월차에 무한 스크롤 타일 배경으로 대체되어 제거됨 |

### 2주차
| 항목 | 상태 | 비고 |
|------|------|------|
| 충돌 감지 + 피해 시스템 | ✅ | OnTriggerEnter2D, 무적시간 |
| 플레이어 HP | ✅ | OnHpChanged 이벤트, HpBar 자기등록 |
| 기본 자동 발사체 | ✅ | PlayerWeapon + Projectile |
| 게임 루프 (생존/사망) | ✅ | GameState.GameOver, GAME OVER 텍스트 |

---

## 2개월차 — 게임 시스템 완성
| 항목 | 상태 | 비고 |
|------|------|------|
| 웨이브 시스템 | ✅ | ScriptableObject WaveData, Resources.Load 자동 참조, startTime 기반 전환 |
| 레벨업 + 업그레이드 선택 UI | ✅ | UpgradeData ScriptableObject, UpgradeManager 이벤트 기반, UI 자기등록 패턴 |
| 무기 2종 추가 | ✅ | BombWeapon — 주기적 범위 폭발, SpatialHashGrid.QueryNeighbors + _hitBuf 재사용, UnlockBomb 업그레이드로 해금 |
| 적 3종 추가 | ✅ | EnemyMover 재사용, WaveData에 enemyPrefab 필드 추가, 웨이브별 프리팹 지정 |
| 기본 UI (HP바, 타이머, 레벨) | ✅ | HpBar + ExpBar 자기등록 패턴, 레벨 텍스트 표시 |

---

## 3개월차 — 최적화
| 항목 | 상태 |
|------|------|
| Object Pool (타입별 Generic Pool) | ✅ | Dictionary<prefab, Queue> 구조, Get/Return, 이중반납 방지 |
| Spatial Hashing 충돌 시스템 | ✅ | Dictionary<셀, List<Enemy>>, 이동 시 셀 갱신, FindNearest 인접 셀만 탐색 |
| GPU Instancing (DrawMeshInstanced) | ✅ | EnemyInstanceRenderer, 타입별 단일 Draw Call, SpriteRenderer 제거 |
| 성능 측정 (500 / 1000 / 2000 적 FPS) | 🔄 | ProfilerMarker 삽입 완료, Job System 도입 전 before 수치 측정 중

### 성능 측정 기록 (Job System 적용 전 — Before)
| 적 수 | QueryNeighbors Time ms | BuildMatrices Time ms | FPS |
|-------|----------------------|----------------------|-----|
| 100   | 0.6 ~ 1.0            | 0.01                 | ~200 |
| 500   | ~25                  |                      | ~30 |
| 1000  | ~110 (PlayerLoop 90ms의 88%) → Job 후 0.6ms |  | <10 → ~200 (Job System 적용 후) |
| 2000  | Job 후 1.6ms (Gather 0.5 + Schedule 1.1) |  | ~40 (Job System 적용 후) |

### 병목 분석
- 1000마리 기준 PlayerLoop 90ms 중 QueryNeighbors 79ms (88%)
- 원인: EnemyMover.Update()에서 적마다 매 프레임 QueryNeighbors 호출 → O(n × 밀도)
- 해결 방향: Job System + Burst로 병렬화 → 예상 30~50 FPS (6코어 기준)

### ProfilerMarker 삽입 위치
| 마커 | 파일 |
|------|------|
| SpatialHash.FindNearest | SpatialHashGrid.cs |
| SpatialHash.QueryNeighbors | SpatialHashGrid.cs |
| EnemyInstanceRenderer.BuildMatrices | EnemyInstanceRenderer.cs |
| EnemyInstanceRenderer.Draw | EnemyInstanceRenderer.cs |
| Rewind.Record | RewindManager.cs |
| Rewind.CaptureEnemies | RewindManager.cs |

---

## 4개월차 — Time Rewind
| 항목 | 상태 |
|------|------|
| Circular Buffer 구현 | ✅ | FrameSnapshot 고정 배열, 0.05초 간격, 5초치 100개 |
| 능동 Rewind (Shift, 5초, 쿨타임 30초) | ✅ | Shift 입력, DoRewind 코루틴, 쿨타임 관리. 쿨타임 감소를 GameState 가드 아래로 이동 — 업그레이드 패널(timeScale=1 유지)에서 새던 것 수정 |
| 사망 Auto-Rewind (스테이지당 1회) | ✅ | OnDead 구독, charges 1회 차감 후 GameOver |
| 엣지케이스 (스폰/소멸 오브젝트 diff) | ✅ | EntityId 추적, 3-케이스 diff (A/B1/B2/C), ForceRestore |

---

## 5개월차 — 폴리싱
| 항목 | 상태 | 비고 |
|------|------|------|
| 버그 수정 / 밸런스 | 🔄 | 한 틱 다중 레벨업(폭탄 다중 처치) 시 업그레이드 선택 1회만 되던 버그 — UpgradeManager 레벨업 큐잉(_pendingLevelUps) + AddExp while + 패널 깜빡임 수정. 밀집 시 적이 플레이어 반대로 드리프트하던 버그 — separation 클램프(_maxSeparation) + SeparationJob 유령 슬롯 순회(Count) 수정 |
| 도형 비주얼 다듬기 | 🔄 | URP Bloom 후처리(@PostFX Volume) + 어두운 배경 + 도형 네온 색상으로 글로우. 적/플레이어/발사체/위성 색 조정. Orbiter 연출(사인파 자전·거리 펄스 + TrailRenderer 잔상). 타입별 모양 차별화는 추후 |
| 데미지 폰트 | ✅ | DamageText.cs TMP 3D + 상승/페이드, 적 흰색/플레이어 빨간색, Resources.Load 캐싱 |
| 적 사망 파티클 이펙트 | ✅ | EnemyBase.OnDead()에서 Instantiate + Managers.Resource.Destroy, 타입별 프리팹 Inspector 지정 |
| 적끼리 밀어내기 (Separation Steering) | ✅ | SpatialHashGrid.QueryNeighbors + EnemyMover 이동벡터 합산, _neighborBuf 재사용으로 GC 없음 |
| GameOver UI 버그 수정 | ✅ | GameManagerEx.OnStateChanged 이벤트 추가, GameScene이 GameOver 상태일 때만 텍스트 표시 |
| Rewind 시각 연출 | ✅ | RewindVolumeController — URP Post Processing Volume weight 페이드, unscaledDeltaTime 사용 |
| 플레이어 피격 화면 연출 | ✅ | HitVignette — 화면 테두리 붉은 섬광 후 페이드(hold 0.05s→fade 0.35s, unscaledDeltaTime). PlayerController.OnHit 이벤트 신설(무적/State 가드 통과분만 발화) + UI 자기등록 구독. 테두리는 UISpriteFactory.EdgeGlow 9-slice — 단일 텍스처를 늘리면 1080×1920에서 좌우가 상하보다 얇아지는 걸 border로 회피. UITheme.HitFlash 토큰 신설(Danger는 핑크빛). Canvas 직속 배치로 SafeArea 밖 노치까지 덮고, 풀스크린이라 raycastTarget 해제 필수(조이스틱 드래그 차단 방지). HitVignetteGenerator([MenuItem]) |
| UpgradePanel nameText 정렬 수정 | ✅ | 긴 이름 왼쪽 쏠림 현상 — nameText Width 확장 |
| Rewind Lerp 보간 | ✅ | DoRewind() 스냅샷 쌍 Lerp — 플레이어+적 위치 보간, 신규 적은 eFrom=eTo 폴백, SpatialHash 갱신 스킵 |
| 폭발형 적 (마름모, 사망 시 범위 피해) | ⬜ | 기획서 4번째 적 타입 — 게임 다양성 |
| 기본 효과음 (발사/피격/사망) | ✅ | SoundManager 기동(Managers.Init에 sound.Init). 단일 진입점 PlayEffect + 키별 unscaledTime 스로틀(대량 적 폭주 방지). 발사/적피격/플레이어피격/적사망/폭발 5종 연결, 적 사운드는 State==Playing 가드 |
| 데이터 주도 무기 시스템 (ScriptableObject) | 🔄 | **1·2단계 완료**: ①토대 — WeaponData(abstract SO)▸ProjectileWeaponData/AoEWeaponData 상속, WeaponBase/WeaponManager(SO→런타임 복사로 오염 방지). ②다중 무기 — WeaponData.AddTo 팩토리, WeaponManager List/AddWeapon/HasWeapon, UnlockBomb→AcquireWeapon 일반화(보유 에셋 단위 제외), 스탯 업그레이드 전체 무기 적용(누적 배율). 데이터만으로 RapidShot 무기 추가 시연. ③신규 행동 무기 — Orbit(회전 위성, SpatialHash 틱 데미지), Lightning(연쇄 번개 — SpatialHash 연쇄 탐색 + LineRenderer 절차적 지그재그 볼트, 아트 0). 무기 프리팹 Prefabs/Weapon/로 정리. 다음: Whip. **2.5단계(무기별 개별 레벨업) 보류** |
| 디버그/치트 컨트롤러 | ✅ | DebugController — 무기 즉시 획득(데이터 주도 WeaponData[])/적 스폰/강제 레벨업/무적 토글, OnGUI 키 안내. 개발 반복·데모 녹화용. **릴리스 포함으로 전환**(포폴 빌드에서도 시연 필요) — `#if UNITY_EDITOR` 게이트를 DebugController·PlayerController(DebugInvincible)·UpgradeManager(DebugSkipUpgrades) 5곳에서 일괄 해제(부분 해제 시 릴리스 컴파일 실패). 오버레이는 F1 토글 기본 OFF로 심사자 화면 보호, 치트키는 토글과 무관하게 상시 동작. GameScene.OnGUI(Enemies/FPS)를 DebugController로 흡수 — 좌표 소유자를 하나로 만들어 상단 HUD 밴드 겹침 해소, FPS는 timeScale=0에서 Infinity로 찍히던 것을 unscaledDeltaTime으로 교정 |
| Unity Job System (여유 있으면) | ✅ | SeparationJob [BurstCompile] IJobParallelFor — 1000마리 79ms → 0.6ms, FPS <10 → ~200 |
| 로비/게임/결과 씬 분리 | ✅ | 멀티 씬(Title/GameScene/Result) + SceneManagerEx 활용, 최고기록(BestScore/BestTime) GameData JSON 저장/로드, 진짜 GameOver(Auto-Rewind 소진)에서만 Result 전환 |
| 메타 상점 (골드 경제) | 🔄 | **2단계 확장 완료**: 적 처치 골드(EnemyBase._goldReward, RunGold→CommitResult 뱅킹), 타이틀 상점. 데이터 주도 — ShopItemData(SO, Resources/Shop/)를 에디터에서 추가하면 상점 자동 등장. ShopService(로드/구매/런 시작 파생), GameData 구매목록 List<ShopPurchase> JSON 저장, UI_ShopPanel/Cell 셀 동적생성. 항목 6종(시작HP/데미지/Rewind 쿨다운·기록시간/공속/이속): Rewind은 Circular Buffer 크기·쿨다운에 런 시작 적용, 공속은 WeaponManager 공통 배율·이속은 Player 시작 속도에 적용. 상점 무기 해금은 단순화 위해 제거(무기는 레벨업 자유 획득). **2단계 축 확장 완료 — 총 9종**: 신규 3종 Range(WeaponManager.UpgradeRange 재사용, 3줄 배선)·RewindCharge(자동 부활 1→3회)·RewindGrace(되감기 직후 무적 0→1.8s, PlayerController.GrantInvincible 신설 — 되돌린 자리에서 즉시 재피격되던 구조적 문제 해소). enum 끝에 추가해 기존 에셋 인덱스 보존, Grace는 기본 0이라 미구매 시 회귀 없음. **경제 보수**: 골드 싱크 14,910→23,680G — 기존엔 퀘스트 일회성 보상(~25,000G)만으로 상점을 전부 사버려 메타가 조기 종료됐음. Rewind 계열이 4종이 되어 "메타 프로그레션 = 되돌리기 중심"이 화면에 드러남. ShopAssetGenerator([MenuItem])로 에셋 생성(파일명 PascalCase·id snake_case 관례 준수). **셀 가독성**: 빈 장식이던 하단 골드 스트립을 실제 가격표로(가격을 본문 라벨에서 분리해 _priceLabel 로). 다음: 스탯창 |
| QUEST 시스템 (메타 누적 도전과제) | ✅ | 영구 티어형, 수동 수령. 평생 스탯(LifetimeKills/Rewinds/Gold, GameData) 누적 → CommitResult() 정산 1지점에서 판정. RunKills(EnemyBase.OnDead)/RunRewinds(RewindManager.DoRewind) 카운터 후킹. 데이터 주도 QuestData(SO, Resources/Quests/, ShopItemData 패턴 복제) + QuestService(Current/ReachedTier/Claim). 4종: SLAYER/SURVIVOR/TIME TRAVELER/COLLECTOR(한글 폰트 미지원으로 영문 통일). 타이틀 로비 퀘스트 패널(UI_ShopPanel/Cell 패턴 복제, 진행바+수령버튼). game-designer→planner→coder→ui-artist 멀티에이전트 파이프라인으로 end-to-end 구현(해커톤 AI활용 쇼케이스). 레이아웃 버그 3라운드 수정(셀 클리핑=Content sizeDelta 미고정, 진행바 흰박스/풀게이지=Filled+sprite 상호작용) |
| 코드기반 UGUI 폴리싱 (UI Kit + 상점 리스킨) | 🔄 | **상점·업그레이드 패널 리스킨 완료**(외부 리소스 0). UISpriteFactory(절차적 라운드+외곽선 9-slice) → UITheme(디자인 토큰 단일 출처)+UIBuild(공유 헬퍼) → [MenuItem] 에디터 생성기(ShopUIGenerator/MenuBackgroundGenerator/UpgradePanelGenerator)로 씬 in-place 리스킨(기존 UI 로직 무수정). 절차 스프라이트는 직렬화 안 되므로 런타임 재생성 컴포넌트(UIProceduralSprite)로 적용 — KB 엔트리화. game-ui-artist 에이전트+ui-kit 스킬로 하네스화. 다음: 타이틀/결과/HUD |
| 모바일 세로(포트레이트) 전환 | 🔄 | game-designer 판단(타깃 모바일 스튜디오·레퍼런스 세로·UI 유동적) → 세로 확정. **검증 통과**: 게임뷰 1080×1920, 카메라 ortho 10. EnemySpawner 스폰 반경을 카메라 코너 밖 런타임 계산으로 변경(화면비 무관 화면 밖 스폰). 업그레이드 패널 세로 스택 재배치. **조작 전환 완료**: VirtualJoystick(좌측 플로팅·아날로그·키보드 OR·모달/Rewind 시 Cancel), 오토에임은 기존 SpatialHash 유지. 링/노브 절차 스프라이트(UISpriteFactory.Circle/Ring). **타이틀 로비형 완료**: 상단 재화바(골드칩+SafeAreaFitter 노치 대응)·중앙 로고/기록카드·하단 SHOP/PLAY. UI 영어 통일. **상점 팝업 세로 재튜닝 완료**(Codex, PopupSize 960×1320 등). **인게임 HUD 완료**: HpBar/ExpBar 리스킨(핸들 숨김, Lv 배지를 XP 바 좌측으로), 상단 Score/Timer(WaveManager.GameTime 라이브)/Gold칩(비율 앵커로 겹침 해소), 우하단 Rewind 버튼(쿨다운 링 Filled radial + Auto 차지 pip, RewindManager.TryActiveRewind로 터치 발동). 다음: 결과 화면 세로 → 밸런스 |

| 보스 시스템 (공격 패턴 + Rewind 연계) | 🔄 | **Stage 1~6 완료 = 최소 출하선**. 데이터 주도 `BossData`(SO) — `patternSequence[]`로 패턴 순서·수치를 데이터화해 2번째 보스를 **C# 0줄**로 만들 수 있게 설계. `BossController : EnemyBase` 직상속(EntityId/스냅샷/무기판정 재사용), GPU 인스턴싱·SeparationJob은 의도적 제외(개체별 색 제어 + 되감기 결정성). 패턴 상태머신 Idle→Telegraph→Execute, 장판(위치 고정)·방사탄(풀링 탄막) 2종. **Rewind 연계가 핵심** — 패턴 순서 고정으로 되감기가 "재도전"이 되고, 보스 HP도 함께 되감겨 그 자체가 남용 방지 장치. `BossSnapshot`을 `FrameSnapshot`에 프레임당 1개 배치(EnemySnapshot 확장·별도 링버퍼 대비 근거는 아키텍처.md). 등장은 `WaveData.isBossWave`로 기존 웨이브 파이프라인에 편입(별도 씬 없음) + 되감기 재전진 시 중복 스폰 가드. 보상=EXP/골드/Auto-Rewind 충전+1/화면 정화, 퀘스트 `BossKills` 티어형. 상단 보스 HP바·WARNING 배너(UIHudLayout 밴드 소유권 도입으로 Pause 버튼 좌표 충돌 해결 + `Tools/UI/Check HUD Overlap` 회귀 검사기). **Stage 7~9 추가 완료** — BOSS RUSH 데모 진입(새 씬 없이 `GameScene.BossRush` 플래그 + `WaveManager.JumpTo`로 보스 직전 시각 점프, 무기 프리셋 지급. 심사·면접·GIF용. 데모 판은 `CommitResult` 제외해 최고기록·퀘스트 오염 방지) / 돌진 패턴(텔레그래프 조준선 = 실제 이동거리와 동일 길이, 방향은 예고 종료 시점 고정 → `BossSnapshot.lockedDir`) / 페이즈2(HP 50% 교차 시 쿨다운 ×0.7·방사탄 12→16·색 전환+흔들림 1회. 연출은 `UpdatePhase`, 상태 복원은 `SetPhase`로 분리해 되감기 중 재발동 없음. `phase2HpRatio=0`이면 전체 비활성 → SWARMLORD 코드 0줄 보장). 스프라이트 Order in Layer 전수 정리 + `에디터_설정.md` 기록(잡몹은 인스턴싱이라 정렬 불가 명시). **페이즈 2 보강** — 쿨다운 배율만으론 전환이 체감되지 않아(한 바퀴 22.6초 → 전투당 공격 5~6회뿐이라 25% 단축이 표본에 안 잡힘) `phase2SpeedMult`·`phase2DamageMult` 축 2개 추가 + 기본 쿨다운 전면 단축(한 바퀴 22.6→14.8초). 신규 필드는 기존 에셋에 0으로 들어오므로 `> 0f` 가드. 돌진 속도는 제외 — 조준선 길이(`chargeSpeed × chargeDuration`)와 묶여 있어 예고와 궤도가 어긋난다. **Stage 10 SWARMLORD는 취소** — 신규 패턴 0인 리스킨이라 "콘텐츠 깊이 얕음"이라는 약점을 못 건드리고, 데이터 주도 증명은 Weapon/Shop/Quest/Wave로 이미 4회 했다. 남은 기간을 밸런스·GIF·문서화에 투입하는 판단 |

| 타임 볼트 (보물상자 → 슬롯 룰렛 골드) | ✅ | 필드 상자를 몸으로 주우면 게임이 멈추고 슬롯 팝업 → 골드 지급. 서바이버 이동 동기에 "위험을 무릅쓰고 주우러 간다"를 추가. **핵심 설계 = 결과 선행 + 역산** — `TreasureReward.Roll()`이 당첨 칸을 먼저 확정하고 `UI_TreasurePopup`은 결과를 인자로 받아 착지만 시킨다(감속 시작 시 `2바퀴+forward` 칸 수 역산). 애니가 멈춘 자리를 결과로 읽는 구조가 아니라 프레임 드랍이 확률을 왜곡하지 않고, **탭 스킵이 공짜로 성립**(역산이 현재 마커 기준이라 착지 칸 불변). 지급은 `BeginHighlight()` 한 곳뿐이라 이중 지급 경로 없음. **되감기 대응 구현 비용 0** — `FrameSnapshot`에 상자도 `RunGold`도 없어 무한 재추첨이 구조적으로 불가능("상자 획득은 되돌아가지 않는다" = `BossProjectile.ClearAll()`과 동급 예외). 상자는 풀링·Collider 없이 `Instantiate` + `sqrMagnitude` 거리 폴링(풀 DontDestroyOnLoad 정리 이슈·Physics 레이어 4조합 규칙 회피). 스폰 40초 후 첫 등장/50~70초 간격/최대 2개/보스경고 중 스킵/수명 25초, 보스 처치 시 확정 드롭. 보상 16칸 균등(20×6·40×5·80×3·150·300) 기대값 63G + 시간배율 ×1/×1.5/×2. 팝업은 `UpgradePanelGenerator` 패턴의 `[MenuItem]` 생성기(TreasurePopupGenerator)로 in-place 생성 — 절차 스프라이트 100% `UIProceduralSprite` 위임. **캔버스 좌표계 함정**: GameScene 메인 Canvas는 Reference 800×600·match 0.5라 1080×1920에서 실좌표가 ~520×924 — 1080 기준으로 튜닝하면 셀이 화면보다 넓어진다(셀 64/spacing 8로 산정). `GridLayoutGroup`은 비활성 자식을 `rectChildren`에서 빼므로 안쪽 9칸은 `SetActive(false)` 대신 Image만 끄고 CenterArt로 덮음. 프리팹은 `Assets/Resources/Treasure/`(경로 로드하는 프리팹만 Resources에 두는 DamageText 선례). 상자 스프라이트는 AI 생성(`TreasureChestSciFi` — 원본 `ArtSource/Generated/Source`, 최종 투명 PNG `Assets/Art/Sprites/Generated`로 분리하는 기존 규격). 재추첨(REWIND ↺ — 쿨다운 소모로 1회 무르기)은 훅만 열어두고 보류 |
| 무한 스크롤 배경 (GL 그리드 → 타일 텍스처) | ✅ | `ScrollingBackground` — 카메라 자식 쿼드 2장의 `mainTextureOffset`을 카메라 월드좌표로 밀어 무한 스크롤(드로우콜 2, 오브젝트 증식 0). 쿼드를 카메라 자식으로 둬 `CameraController.LateUpdate`(Lerp+셰이크) 결과가 트랜스폼으로 전파 → 한 프레임 밀림 없음. UV 갱신은 `beginCameraRendering`에서 처리해 LateUpdate 실행 순서에 무의존. 오프셋 `Mathf.Repeat`로 감아 장시간 플레이 float 정밀도 방어. 베이스(Opaque, 12유닛)+디테일(Additive, 17유닛) 2겹, **타일 크기를 서로소로 잡아 반복 주기 LCM 204유닛**. 적이 `DrawMeshInstanced`라 sortingOrder가 안 통해 정렬은 renderQueue(Priority −50)+z=+5로 처리. 텍스처는 GPT 생성 seamless 타일, 3×3 타일링 후 리샘플로 POT 1024 축소(가장자리 보간이 반대편을 참조해 seam 보존). `GridBackground` 제거 |
| AI 생성 전투 스프라이트 적용 | 🔄 | 4방향 대칭 네온 규격으로 플레이어·기본/속도/탱커 적·MONOLITH 보스·회전 무기 제작. 원본은 `ArtSource/Generated/Source`, 최종 투명 PNG는 `Assets/Art/Sprites/Generated`로 분리. SpriteRenderer 에셋은 PPU로 크기를 보정하고, GPU 인스턴싱 적은 전용 Material의 Base Map Scale/Offset으로 여백을 보정. 현재: 일반/연사 탄환 리소스 추가 예정. |

### 세로 전환 남은 TODO (다음 세션)
- ✅ **UI 레이어 시스템** — `UILayer` enum(Background=0/Hud=100/Modal=300, UIManager 소유) + `UILayerCanvas`(overrideSorting+GraphicRaycaster GetOrAdd) + 6개 생성기가 패널별 레이어 배정. sibling 애드혹(SetAsLastSibling/TopmostOnEnable) 제거 → 렌더 순서를 상수 sortingOrder로. 모달이 항상 HUD 위(업그레이드 모달 순서 버그 해결). 빈 UIManager 스텁 활용.
- ⬜ **업그레이드 패널 높이** — 폭은 화면 86% 상대라 안전하나 `PopupHeight 680` 고정값 세로 재확인.
- ✅ **결과(Result) 화면** — 세로 카드형 리스킨(ResultSceneGenerator): "RESULT" + 결과 카드(이번 판 Score/Time/Gold 강조 + Best 보조, ContentSizeFitter로 hug) + 하단 RETRY/TITLE, Safe Area, UILayer.Hud. 기존 ResultScene 로직 무수정.
- ✅ **인게임 Pause 화면** — 전용 오버레이(UILayer.Pause=400, HUD/모달 위): 실시간 스텟 6종(데미지%/공속%/이동속도/최대HP/사거리%/무기수, WeaponManager getter 신설) + Resume + 타이틀 복귀. ESC/우상단 버튼 토글, GameState.Paused+timeScale=0 병행 정지. PauseController(항상 켜짐)와 _panelRoot(자식) 분리로 ESC 폴링 유지. **연쇄 버그 수정**: 씬 전환 시 DontDestroyOnLoad 풀 잔존 오브젝트 파괴(ObjectManager.Clear 실물 파괴) + static EnemyBase.Registry 초기화(MissingReference) + SoundManager 기존 @SoundRoot 재사용 시 _audioSources 재바인딩(NRE) — KB 엔트리화.
- ✅ **빌드 설정 세로 고정** — 첫 스탠드얼론 빌드 착수 중 발견: `fullscreenMode: 1`(FullScreenWindow) + `defaultIsNativeResolution: 1` 조합이라 **`defaultScreenWidth/Height`를 읽지도 않고** 모니터 네이티브(가로)로 전체화면 실행돼 세로 UI 작업이 통째로 무효화되는 상태였다. 해상도 숫자만 고쳐선 안 고쳐진다. → `fullscreenMode: 3`(Windowed) + `defaultIsNativeResolution: 0` + 540×960(9:16, CanvasScaler 1080×1920의 정확히 절반, 1080p 모니터에 창으로 수납) + Web 해상도 동일 + `defaultScreenOrientation: 0`(Portrait) + 가로 오토로테이트 차단 + `allowFullscreenSwitch: 0`(Alt+Enter로 레이아웃 파괴 방지). `resizableWindow: 0`은 기존값 유지. productName/companyName을 `Rewind Survivors`/`Gyeonghun Choi`로 지정 — **부수효과로 `Application.persistentDataPath`가 바뀌어 기존 세이브가 고아가 된다**(첫 실행 경험 검증 기회로 삼고 이관하지 않음).
- ✅ **WebGL 빌드 + Pages 배포 파이프라인** — 해커톤 제출 요건이 웹/모바일 실행본이라(PC `.exe` 접수 불가) WebGL로 확정. Android는 모듈·SDK·서명·실기기가 D-4에 신규 리스크라 배제. `WebGLBuilder`(Tools/Build/WebGL Build)가 설정 적용까지 전담: **`decompressionFallback` 필수** — Pages는 `Content-Encoding` 헤더를 못 실어 압축 파일 해제에 실패하고 로딩 바에서 멈춘다. 출력 경로는 `docs/`(Pages는 루트 또는 /docs만 서빙). 기본 웹 템플릿이 캔버스를 540×960**px 고정**으로 박아 창보다 커지면 상단 HUD가 잘리므로 `Assets/WebGLTemplates/Portrait` 신설 — 9:16 유지 + `min(100dvh, 100vw×16/9)`로 창에 맞춤, 포커스 아웃라인 제거, 로드 후 `canvas.focus()`로 클릭 없이 키 입력 수신. 검증: 3씬 전환·전투·보스·결과·세이브(IndexedDB) 정상, **런타임 에러 0건**. `Hidden/CoreSRP/CoreCopy` 미지원 로그는 렌더링 영향 없음.
- ✅ **BGM + 볼륨 슬라이더** — 죽은 `SoundManager`에 재생 경로를 붙여 미완성 흔적 제거. 조사 결과 `BGMOn`/`EffectSoundOn` bool은 **아무도 set하지 않는 죽은 스위치**라 삭제하고 볼륨 float 한 벌로 단일화(파생 프로퍼티 안은 볼륨 0 저장 시 소스가 Stop 상태로 굳어 복구 불가라 탈락). **불변식: BGM은 볼륨 0이어도 항상 Play — 음소거는 volume 0이지 Stop이 아니다.** 브라우저가 첫 제스처 전까지 AudioContext를 잠그고 그 상태의 `Play()`는 도입부만 유실시키며 `isPlaying`이 true라 감지도 안 되므로, `BgmStarter`가 첫 입력을 Polling 감지한 뒤 재생하고 자기 컴포넌트를 Destroy(플랫폼 분기 없이 단일 경로 — 에디터 검증이 곧 WebGL 동작). 볼륨은 `ApplyVolume()` 단일 진입점, 세이브는 Pause 패널 `OnDisable` 1회(드래그 중 IO 폭주 방지). 효과음은 `PlayOneShot`이 호출 시점 volume을 곱하는 성질을 이용해 **호출부 9군데 무수정**. 세이브에 `SchemaVersion` 도입 — float은 null 가드가 불가능하고 0이 "미저장"과 "음소거"를 구분 못 해 구버전 로드 시 전 게임 무음이 된다(KB 신규 엔트리화). BGM 에셋은 5분 18초·7.35MB → 60초·1.27MB로 교체(웹 첫 로딩 시간 직결).
- ✅ **밸런스 — 해커톤 심사판 튜닝** — 기준을 "장기 밸런스"가 아니라 **"1~3판 안에 되감기 정체성이 전달되는가"**로 잡고 조정(심사자는 웹 링크로 길어야 3판). 진단: ① 보스가 15초에 등장해 초기 DPS 20으로는 못 잡는 스펀지 ② 보스 공격 주기 7.5초로 전투가 비어 보임 ③ 정체성인 되감기 항목이 500~1500골드로 가장 비싸 심사 세션에 안 보임 ④ 보스 격파 후 무한 정체 — `CommitResult`가 사망 시에만 호출돼 **안 죽으면 골드가 은행에 안 들어감**. 조치(전부 SO 에셋, **C# 0줄**): 기본무기 20뎀/1발 → **15뎀/2발**(DPS 30 유지, 타격 피드백 2배 — 3발은 Rapid와 구분이 안 돼 되돌림) / 보스 15초→**30초**, HP 3000→**2400**, 쿨다운 5·6·8→**3·3.5·5**(피해 수치는 유지 — 회피 가능성이 곧 재도전 가능성), 골드 150→**400** / RewindCharge 1500→**250**, RewindCooldown 600→**350**·티어당 −3→**−5초**(가시성 높은 항목부터 싸게) / TIME TRAVELER 10회→**3회** / 신규 `WaveData5`(90초, HP 30)로 판이 끝나게 함 / 구현돼 있으나 정규 플레이 풀에 없던 **Orbit·Lightning 언락 에셋 2종 추가**(BOSS RUSH 프리셋에만 있었음). 가격을 내리고 수급은 안 건드린 이유 — `costs`는 저장된 tier와 독립이라 **기존 세이브가 안전**하고, 수급을 만지면 COLLECTOR 목표치·상자 룰렛 가치가 연동돼 흔들린다. 검증: 보스 격파 70초, 보스전 되감기 1회 사용(의도 달성).
- ✅ **업그레이드 표기 정정** — 이름에 박힌 탭·개행 제거(`"Damage Up\t\t"`, `RapidUnlock`), 설명과 실제 값 불일치 수정(Damage x1.5→**x1.25**, Range x1.3→**x1.2**). 값이 아니라 표기를 맞춘 것 — 값 변경은 밸런스 결정이라 별도로 다룬다.
- ✅ **[버그] 보스 예고원이 배경에 가려짐** — 배경을 타일 쿼드(MeshRenderer)로 교체하며 발생한 회귀. `sortingOrder`가 z보다 우선하는데 배경 기본값 0 > 예고원 −20/−19라 통째로 가려졌다. 플레이어(10)·발사체(5)는 0보다 커서 멀쩡해 **음수 Order만 사라지는** 형태로 나타났고, 흔들림·판정은 정상이라 로직 버그로 오인하기 쉬웠다. **`MeshRenderer`는 Order 필드를 Inspector에 노출하지 않아** 씬에서 고칠 수 없고 코드로만 지정 가능 — `ScrollingBackground.Awake`에서 −100/−99 지정. KB 엔트리화 + `에디터_설정.md` Order 표에 배경 등재(표에 없던 것이 원인).
- ✅ **적 밀도 구간 + 스폰 상한** — 두 문제가 같은 뿌리였다. ① 탱커가 한 방에 죽음 — `Enemy_Tanker` 프리팹을 쓰지만 **HP는 프리팹이 아니라 WaveData가 덮어쓴다**(`Spawn(prefab, hp, speed)`). 값이 8이라 이름만 탱커였다 → W3 20 / W4 25 / W5 45로 올리되 **속도는 3→1.8로 낮춤**(단단하면서 빠르면 회피 불가라 "어렵다"가 아니라 "불공정하다"가 된다. 탱커=느린 벽, Speeder=빠르고 무름으로 역할 분리). ② GPU 인스턴싱·Job System을 만들어놓고 **보여줄 장면이 없었다** — 스폰(2.5/초)과 처치 속도가 비슷해 개체가 안 쌓였기 때문. `WaveData6`(140초, 0.5초마다 12마리 = 초당 24) 추가로 **의도적으로 균형을 깨는 밀도 구간**을 만듦. ③ `EnemySpawner`에 `MaxAliveEnemies = 300` 상한 — 프레임 방어이자 **되감기 버퍼가 프레임당 전 개체를 복사**하므로 상한 없이는 메모리도 함께 무너진다.
- ✅ **업그레이드 패널 — 무기 언락 카드 강조** — 무기 획득이 스탯 1.25배보다 체감상 압도적인데 카드가 똑같이 생겨 가치가 안 읽혔다. 골드 테두리 + "NEW WEAPON" 배지. **로직은 `_weaponHighlights[i].SetActive(type == AcquireWeapon)` 토글만 하고 생김새는 전부 생성기가 소유** — 강조 방식을 바꿀 때 생성기만 고치면 된다. 카드 크기·위치는 불변(무기/스탯 카드 크기가 달라지면 3택 레이아웃이 흔들림). 배경 톤 차별은 버튼 눌림 피드백을 가려서 제외, 애니메이션은 정지 화면 피로를 고려해 제외.
- ✅ **시야 확대 + 디버그 오버레이 웹 대응** — 카메라 orthographic size 10→12(세로 20→24유닛). 스폰 반경·배경 타일은 카메라 값에서 계산해 자동 추종하지만 **무기 사거리·보스 리쉬는 월드 고정값이라 화면 대비 상대적으로 짧아진다**(KB `design-values-vs-portrait-screen`와 같은 종류). F1 오버레이는 **OnGUI 내장 폰트에 한글 글리프가 없어** WebGL에서 빈 칸으로 나오던 것을 영문으로 고정하고, 세로 화면 폭에서 자동 줄바꿈되며 아랫줄과 겹치던 문제를 두 줄 분리 + 폭 `Screen.width-20`으로 해결.
- ✅ **[버그] 중간 퇴장 시 판 결과 유실 + 타이틀 BEST 미표시** — ① 정산(`CommitResult`)이 **사망 경로 한 곳에만** 있어 Pause→타이틀로 나가면 그 판의 골드·기록·퀘스트 진행이 통째로 버려졌다. 심사자가 한 판을 끝까지 안 하는 건 흔한 행동이라 메타 시스템 전체가 고장난 것처럼 읽힌다. `PauseController.GoTitle`에 정산 추가 — 단 `PlayTime`도 함께 넣어야 한다(사망 경로에서만 갱신돼, 정산해도 **이전 판의 낡은 값**과 비교되어 최고 기록이 안 찍혔다). `GameScene.BossRush` 제외 규칙은 사망 경로와 동일하게 유지(데모 판이 실제 기록을 오염시키지 않도록). ② 타이틀 BEST가 비어 보이던 것은 **로비 계층이 2벌**이고 중복 카드(BestText 없음)가 진짜 카드를 정확히 덮고 있었기 때문. 세 캔버스가 전부 `overrideSorting=1, order=100` **동점**이라 Hierarchy 순서가 무의미했다 — 동점 시 렌더 순서에 보장이 없다는 원칙이 캔버스 레벨에서 재현된 사례. 중복 `CenterGroup`만 삭제(같은 로비 `BottomBar`에 QUEST 버튼이 배선돼 있어 로비 자체는 유지).
- ✅ **스폰 상한 300→2000 + 웹 F1 충돌 차단** — 상한 300은 이 프로젝트가 증명하려는 규모(Job System 2000마리)를 스스로 막는 값이라 2000으로 올리고 `[SerializeField]`로 노출(성능 목표가 아니라 무한 증가 안전장치임을 주석에 명시). 웹에서 F1이 크롬 고객센터를 여는 문제는 템플릿에서 capture 단계 `preventDefault`로 차단 — 이벤트는 흘려보내 Unity 입력은 유지. README 상단의 깨진 GIF 참조(`docs/gameplay.gif` 부재)를 스크린샷 3장 + 플레이·영상 링크로 교체하고, 성능 수치가 **에디터(멀티스레드) 측정값**임을 명시(웹은 Job이 단일 스레드라 동일 수치가 안 나온다).
- ⬜ **밸런스** — 좁아진 세로 시야에 맞춰 무기 사거리 체감·스폰 밀도 미세조정.
- ⬜ **[폴리싱/제출 후] 전 씬 CanvasScaler 1080×1920 통일** — 현재 GameScene만 Reference 800×600(4:3, Unity 기본값 방치)이고 Title/Result/Shop은 1080×1920(9:16). HUD/조이스틱/업그레이드/배경 4개 생성기가 800 기준으로 튜닝돼 있어, 통일하려면 가로(×1.35)·세로(×3.2) **비대칭 배율로 요소별 재튜닝 + 스크린샷 재검증** 필요(단순 2배 아님). 회귀 위험 있는 눈검증 작업이라 **해커톤 제출(2026-08-09) 이후 실기기 테스트와 함께 전용 세션으로** 진행. 재발 방지 가드(생성기가 기존 ScaleWithScreenSize 스케일러 미덮어씀)는 이미 적용됨(387ae71). Pause는 이미 전용 @PauseCanvas(1080×1920)로 분리돼 있어 통일 시 참고 패턴.

### 포폴 우선순위 메모 (2026-06-15 전체 평가)
- **현 상태 판정:** 엔지니어링(Time Rewind 풀링 diff + 측정된 최적화)은 3년차+에 적합/이상. 게임 콘텐츠 깊이는 얕음 → "기술 데모"로 읽힐 위험 + 미완성 흔적(죽은 SoundManager).
- **메울 구멍 2개:** (1) 미완성 흔적 제거, (2) 숨은 기술을 가시화.
- **다음 작업 순서 (ROI):** ① 효과음(구멍 막기) → ② 6개월차 문서화(숨은 기술 드러내기, 최고 ROI) → ③ 데이터 주도 무기 리팩토링(시니어 신호) → ④ 폭발형 적/비주얼(콘텐츠).
- **결론:** 새 기술 스택 추가는 ROI 낮음. 보유 기술을 완성·문서화하는 단계.

---

## 6개월차 — 문서화
| 항목 | 상태 |
|------|------|
| GitHub README (아키텍처 다이어그램) | 🔄 초안 (Mermaid 다이어그램 + 기술 하이라이트 + AI 워크플로, GIF·최종 다듬기 추후) |
| Knowledge Base 구축 (AI 워크플로 자산화) | ✅ | `.claude/knowledge/`(bug-patterns/lessons-learned/workflows). 버그 수정 시 "증상→근본원인→재인식 패턴" 축적, 착수 전 KB 검색 트리거를 CLAUDE.md에 배선. 시드 5건 실제 커밋 근거(07799f3·1ff2f27). agents+skills+knowledge로 AI 하네스 스토리 완성 |
| 성능 측정 결과 정리 (Profiler 캡처) | ⬜ |
| 플레이 / Rewind 시연 GIF | ⬜ |
| 면접 Q&A 정리 | ⬜ |

### AI 도구 공통 설정
- ✅ **Claude·Codex 공통 설정 통합** — `AGENTS.md`를 프로젝트 규칙 단일 원본으로, `.agents/roles/`을 역할 상세 지시 단일 원본으로 지정. Claude/Codex 에이전트는 각 도구 형식의 얇은 어댑터로 축소. 공통 스킬은 `.agents/skills/`에서 관리하고 동기화 스크립트로 Claude 사본을 갱신.
