# Rewind Survivors — 개발 진행상황

## 1개월차 — Unity 재적응 + 기초 루프

### 1주차
| 항목 | 상태 | 비고 |
|------|------|------|
| 프로젝트 세팅 | ✅ | URP / New Input System (Unity 6 기본값) |
| 매니저 프레임워크 | ✅ | Managers, ResourceManager, SoundManager, GameManagerEx, DataManager, ObjectManager, UIManager, SceneManager |
| Define / Utils | ✅ | GameState, CreatureState, SceneType, Sound enum |
| 플레이어 이동 (WASD) | ✅ | New Input System Polling 방식 (Keyboard.current) |
| 카메라 추적 | ✅ | LateUpdate Lerp 방식 |
| 적 기본 구조 | ✅ | EnemyBase (스탯/피해), EnemyMover (추적 이동) |
| 적 스폰 | ✅ | EnemySpawner — 플레이어 자동 참조, 랜덤 위치 스폰 |
| 그리드 배경 | ✅ | URP RenderPipelineManager 방식 (OnPostRender 대체) |

### 2주차
| 항목 | 상태 | 비고 |
|------|------|------|
| 충돌 감지 + 피해 시스템 | ✅ | OnTriggerEnter2D, 무적시간 |
| 플레이어 HP | ✅ | OnHpChanged 이벤트, HpBar 자기등록 |
| 기본 자동 발사체 | ✅ | PlayerWeapon + Projectile |
| 게임 루프 (생존/사망) | ✅ | GameState.GameOver, GAME OVER 텍스트 |

---

## 2개월차 — 게임 시스템 완성
| 항목 | 상태 | 비고 |
|------|------|------|
| 웨이브 시스템 | ✅ | ScriptableObject WaveData, Resources.Load 자동 참조, startTime 기반 전환 |
| 레벨업 + 업그레이드 선택 UI | ✅ | UpgradeData ScriptableObject, UpgradeManager 이벤트 기반, UI 자기등록 패턴 |
| 무기 2종 추가 | ✅ | BombWeapon — 주기적 범위 폭발, SpatialHashGrid.QueryNeighbors + _hitBuf 재사용, UnlockBomb 업그레이드로 해금 |
| 적 3종 추가 | ✅ | EnemyMover 재사용, WaveData에 enemyPrefab 필드 추가, 웨이브별 프리팹 지정 |
| 기본 UI (HP바, 타이머, 레벨) | ✅ | HpBar + ExpBar 자기등록 패턴, 레벨 텍스트 표시 |

---

## 3개월차 — 최적화
| 항목 | 상태 |
|------|------|
| Object Pool (타입별 Generic Pool) | ✅ | Dictionary<prefab, Queue> 구조, Get/Return, 이중반납 방지 |
| Spatial Hashing 충돌 시스템 | ✅ | Dictionary<셀, List<Enemy>>, 이동 시 셀 갱신, FindNearest 인접 셀만 탐색 |
| GPU Instancing (DrawMeshInstanced) | ✅ | EnemyInstanceRenderer, 타입별 단일 Draw Call, SpriteRenderer 제거 |
| 성능 측정 (500 / 1000 / 2000 적 FPS) | 🔄 | ProfilerMarker 삽입 완료, Job System 도입 전 before 수치 측정 중

### 성능 측정 기록 (Job System 적용 전 — Before)
| 적 수 | QueryNeighbors Time ms | BuildMatrices Time ms | FPS |
|-------|----------------------|----------------------|-----|
| 100   | 0.6 ~ 1.0            | 0.01                 | ~200 |
| 500   | ~25                  |                      | ~30 |
| 1000  | ~110 (PlayerLoop 90ms의 88%) → Job 후 0.6ms |  | <10 → ~200 (Job System 적용 후) |
| 2000  | Job 후 1.6ms (Gather 0.5 + Schedule 1.1) |  | ~40 (Job System 적용 후) |

### 병목 분석
- 1000마리 기준 PlayerLoop 90ms 중 QueryNeighbors 79ms (88%)
- 원인: EnemyMover.Update()에서 적마다 매 프레임 QueryNeighbors 호출 → O(n × 밀도)
- 해결 방향: Job System + Burst로 병렬화 → 예상 30~50 FPS (6코어 기준)

### ProfilerMarker 삽입 위치
| 마커 | 파일 |
|------|------|
| SpatialHash.FindNearest | SpatialHashGrid.cs |
| SpatialHash.QueryNeighbors | SpatialHashGrid.cs |
| EnemyInstanceRenderer.BuildMatrices | EnemyInstanceRenderer.cs |
| EnemyInstanceRenderer.Draw | EnemyInstanceRenderer.cs |
| Rewind.Record | RewindManager.cs |
| Rewind.CaptureEnemies | RewindManager.cs |

---

## 4개월차 — Time Rewind
| 항목 | 상태 |
|------|------|
| Circular Buffer 구현 | ✅ | FrameSnapshot 고정 배열, 0.05초 간격, 5초치 100개 |
| 능동 Rewind (Shift, 5초, 쿨타임 30초) | ✅ | Shift 입력, DoRewind 코루틴, 쿨타임 관리 |
| 사망 Auto-Rewind (스테이지당 1회) | ✅ | OnDead 구독, charges 1회 차감 후 GameOver |
| 엣지케이스 (스폰/소멸 오브젝트 diff) | ✅ | EntityId 추적, 3-케이스 diff (A/B1/B2/C), ForceRestore |

---

## 5개월차 — 폴리싱
| 항목 | 상태 | 비고 |
|------|------|------|
| 버그 수정 / 밸런스 | 🔄 | 한 틱 다중 레벨업(폭탄 다중 처치) 시 업그레이드 선택 1회만 되던 버그 — UpgradeManager 레벨업 큐잉(_pendingLevelUps) + AddExp while + 패널 깜빡임 수정 |
| 도형 비주얼 다듬기 | 🔄 | URP Bloom 후처리(@PostFX Volume) + 어두운 배경 + 도형 네온 색상으로 글로우. 적/플레이어/발사체/위성 색 조정. Orbiter 연출(사인파 자전·거리 펄스 + TrailRenderer 잔상). 타입별 모양 차별화는 추후 |
| 데미지 폰트 | ✅ | DamageText.cs TMP 3D + 상승/페이드, 적 흰색/플레이어 빨간색, Resources.Load 캐싱 |
| 적 사망 파티클 이펙트 | ✅ | EnemyBase.OnDead()에서 Instantiate + Managers.Resource.Destroy, 타입별 프리팹 Inspector 지정 |
| 적끼리 밀어내기 (Separation Steering) | ✅ | SpatialHashGrid.QueryNeighbors + EnemyMover 이동벡터 합산, _neighborBuf 재사용으로 GC 없음 |
| GameOver UI 버그 수정 | ✅ | GameManagerEx.OnStateChanged 이벤트 추가, GameScene이 GameOver 상태일 때만 텍스트 표시 |
| Rewind 시각 연출 | ✅ | RewindVolumeController — URP Post Processing Volume weight 페이드, unscaledDeltaTime 사용 |
| UpgradePanel nameText 정렬 수정 | ✅ | 긴 이름 왼쪽 쏠림 현상 — nameText Width 확장 |
| Rewind Lerp 보간 | ✅ | DoRewind() 스냅샷 쌍 Lerp — 플레이어+적 위치 보간, 신규 적은 eFrom=eTo 폴백, SpatialHash 갱신 스킵 |
| 폭발형 적 (마름모, 사망 시 범위 피해) | ⬜ | 기획서 4번째 적 타입 — 게임 다양성 |
| 기본 효과음 (발사/피격/사망) | ✅ | SoundManager 기동(Managers.Init에 sound.Init). 단일 진입점 PlayEffect + 키별 unscaledTime 스로틀(대량 적 폭주 방지). 발사/적피격/플레이어피격/적사망/폭발 5종 연결, 적 사운드는 State==Playing 가드 |
| 데이터 주도 무기 시스템 (ScriptableObject) | 🔄 | **1·2단계 완료**: ①토대 — WeaponData(abstract SO)▸ProjectileWeaponData/AoEWeaponData 상속, WeaponBase/WeaponManager(SO→런타임 복사로 오염 방지). ②다중 무기 — WeaponData.AddTo 팩토리, WeaponManager List/AddWeapon/HasWeapon, UnlockBomb→AcquireWeapon 일반화(보유 에셋 단위 제외), 스탯 업그레이드 전체 무기 적용(누적 배율). 데이터만으로 RapidShot 무기 추가 시연. ③신규 행동 무기 — Orbit(회전 위성, SpatialHash 틱 데미지), Lightning(연쇄 번개 — SpatialHash 연쇄 탐색 + LineRenderer 절차적 지그재그 볼트, 아트 0). 무기 프리팹 Prefabs/Weapon/로 정리. 다음: Whip. **2.5단계(무기별 개별 레벨업) 보류** |
| 디버그/치트 컨트롤러 (에디터 전용) | ✅ | DebugController(#if UNITY_EDITOR) — 무기 즉시 획득(데이터 주도 WeaponData[])/적 스폰/강제 레벨업/무적 토글, OnGUI 키 안내. 릴리스 빌드 제외. 개발 반복·데모 녹화용 |
| Unity Job System (여유 있으면) | ✅ | SeparationJob [BurstCompile] IJobParallelFor — 1000마리 79ms → 0.6ms, FPS <10 → ~200 |
| 로비/게임/결과 씬 분리 | ✅ | 멀티 씬(Title/GameScene/Result) + SceneManagerEx 활용, 최고기록(BestScore/BestTime) GameData JSON 저장/로드, 진짜 GameOver(Auto-Rewind 소진)에서만 Result 전환 |

### 포폴 우선순위 메모 (2026-06-15 전체 평가)
- **현 상태 판정:** 엔지니어링(Time Rewind 풀링 diff + 측정된 최적화)은 3년차+에 적합/이상. 게임 콘텐츠 깊이는 얕음 → "기술 데모"로 읽힐 위험 + 미완성 흔적(죽은 SoundManager).
- **메울 구멍 2개:** (1) 미완성 흔적 제거, (2) 숨은 기술을 가시화.
- **다음 작업 순서 (ROI):** ① 효과음(구멍 막기) → ② 6개월차 문서화(숨은 기술 드러내기, 최고 ROI) → ③ 데이터 주도 무기 리팩토링(시니어 신호) → ④ 폭발형 적/비주얼(콘텐츠).
- **결론:** 새 기술 스택 추가는 ROI 낮음. 보유 기술을 완성·문서화하는 단계.

---

## 6개월차 — 문서화
| 항목 | 상태 |
|------|------|
| GitHub README (아키텍처 다이어그램) | 🔄 초안 (Mermaid 다이어그램 + 기술 하이라이트 + AI 워크플로, GIF·최종 다듬기 추후) |
| 성능 측정 결과 정리 (Profiler 캡처) | ⬜ |
| 플레이 / Rewind 시연 GIF | ⬜ |
| 면접 Q&A 정리 | ⬜ |
