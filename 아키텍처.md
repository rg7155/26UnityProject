# Rewind Survivors — 아키텍처

## 전체 구조 개요

```
Managers (DontDestroyOnLoad)
├── GameManagerEx   — GameState, Score, 세이브/로드
├── ResourceManager — Resources.Load 캐시
├── SoundManager    — BGM / Effect
├── UIManager       — UI 스택 관리 (현재 미사용)
├── ObjectManager   — Object Pool (Get/Return, 프리팹 키 기반)
├── SceneManagerEx  — 씬 전환 (현재 미사용)
└── DataManager     — JSON 직렬화

GameScene (씬 진입점)
├── Awake: Managers.Init()
└── Start: 플레이어 찾기, 카메라 타겟 설정, OnDead 구독
```

---

## GameState 흐름

```
Playing ──레벨업──▶ Paused    ──업그레이드 선택──▶ Playing
Playing ──Shift──▶ Rewinding  ──되감기 완료──▶    Playing
Playing ──사망──▶  Rewinding  ──Auto-Rewind──▶    Playing  (charges > 0)
Playing ──사망──▶  GameOver                        (charges == 0)
```

- `Managers.Game.State` 를 모든 Update()에서 체크
- `GameState.Paused` / `GameState.Rewinding` 동안 PlayerController, PlayerWeapon, EnemyMover, WaveManager, EnemySpawner 전부 멈춤
- Pause/Resume 책임: **UpgradeManager**
- Rewind 책임: **RewindManager**

---

## 경험치 / 레벨업 / 업그레이드 흐름

```
적 처치
  └▶ EnemyBase.OnDead()
       └▶ PlayerController.AddExp(_expReward)
            └▶ _exp >= _expToNextLevel ?
                 └▶ HandleLevelUp()
                      ├── _exp -= _expToNextLevel
                      ├── _level++
                      ├── _expToNextLevel *= 1.3f
                      ├── OnExpChanged 이벤트 발생 (차감된 exp, 새 expToNext)
                      │    └▶ ExpBar.UpdateExp() — 바 리셋
                      └── OnLevelUp 이벤트 발생
                           └▶ UpgradeManager.HandleLevelUp()
                                ├── GameState = Paused
                                ├── PickRandom(3) — UpgradeData 3개 랜덤 선택
                                └── OnUpgradeChoiceReady 이벤트 발생
                                     └▶ UI_UpgradePanel.Show()
                                          └── 카드 3개 표시

카드 클릭
  └▶ UpgradeManager.ApplyUpgrade(upgrade)
       ├── UpgradeData.Apply(player, weapon)
       └── GameState = Playing
```

**경험치 수치:**
- 초기 `_expToNextLevel = 10`, 적 `_expReward = 1`
- 레벨업마다 요구 경험치 ×1.3 (10 → 13 → 17 → 22...)

---

## 웨이브 시스템 흐름

```
WaveManager.Start()
  └▶ Resources.LoadAll<WaveData>("Waves") — 자동 로드
       └▶ startTime 기준 정렬
            └▶ EnemySpawner 찾기 (FindObjectOfType)

WaveManager.Update() [매 프레임, Playing 상태만]
  ├── _gameTime 누적
  ├── CheckWaveTransition() — 다음 웨이브 startTime 도달 시 전환
  └── _spawnTimer <= 0 → SpawnBurst()
           └▶ EnemySpawner.Spawn(hp, speed) × spawnCountPerBurst
```

**WaveData 필드:** `startTime`, `spawnInterval`, `spawnCountPerBurst`, `enemyHp`, `enemySpeed`

---

## 발사체 / 충돌 흐름

```
ProjectileWeapon.Update()   (WeaponManager가 장착·관리 — 무기 시스템 참고)
  └▶ TryFire()
       ├── FindNearest() → SpatialHashGrid.Instance.FindNearest(pos, detectRange)
       └── Fire(dir) → Managers.Object.Get(projectilePrefab)
                         └── Projectile.Init(dir, damage, range, originPrefab)

Projectile.Update() — 사거리 초과 시 ReturnToPool()
Projectile.OnTriggerEnter2D(Enemy)
  └▶ EnemyBase.OnDamaged(damage) → ReturnToPool()
       └▶ _hp <= 0 → OnDead()
            ├── AddExp
            ├── _hp = _originHp  (HP 리셋)
            └── Managers.Object.Return() — 풀 반납

EnemyMover.OnTriggerEnter2D(Player)
  └▶ PlayerController.OnDamaged(contactDamage)
       └▶ _hp <= 0 → HandleDead() → GameState = GameOver
```

**Physics Layer 설정:**
- `Projectile` 레이어 ↔ `Enemy` 레이어: 충돌 ON
- `Player` 레이어 ↔ `Enemy` 레이어: 충돌 ON

---

## 무기 시스템 (데이터 주도)

플레이어는 무기를 여러 개 장착해 동시에 발사한다. 무기 정의는 ScriptableObject로 분리되어, 에셋만 추가하면 새 무기가 생긴다.

### 클래스 구조

```
WeaponData (abstract SO)              공통: weaponName, damage
  ├── ProjectileWeaponData            projectilePrefab, fireRate, range, detectRange
  ├── AoEWeaponData                   interval, radius, explosionEffectPrefab
  ├── OrbitWeaponData                 orbiterCount, orbitRadius, rotationSpeed, hitRadius, hitInterval, orbiterPrefab
  └── LightningWeaponData             fireRate, detectRange, chainCount, chainRange, damageFalloff, boltPrefab

WeaponBase (abstract MonoBehaviour)   Init(WeaponData), UpgradeDamage, 빈 가상 Upgrade*
  ├── ProjectileWeapon                자동 조준 발사
  ├── AoEWeapon                       주기 범위 폭발
  ├── OrbitWeapon                     회전 위성 접촉 데미지
  └── LightningWeapon                 연쇄 번개

WeaponManager (Player 부착)           List<WeaponBase> 관리
```

### 데이터 주도 생성 — 팩토리 메서드

`WeaponData`가 자기 무기 컴포넌트를 직접 부착한다. `WeaponManager`는 무기 종류를 몰라도 된다 (enum switch 없음).

```
WeaponData.AddTo(GameObject owner)   ← abstract
  ProjectileWeaponData → owner.AddComponent<ProjectileWeapon>().Init(this)
  OrbitWeaponData      → owner.AddComponent<OrbitWeapon>().Init(this)
  ...

WeaponManager.AddWeapon(WeaponData data)
  ├── var w = data.AddTo(gameObject)     ← 다형성으로 올바른 무기 생성
  ├── 누적 스탯 배율 재적용 (아래 참고)
  └── _weapons.Add(w)
```

### 무기 획득 흐름

무기 획득은 업그레이드 시스템에 통합되어 있다 (`UpgradeType.AcquireWeapon`).

```
레벨업 → UpgradeManager.PickRandom(3)
  └── AcquireWeapon UpgradeData 중, WeaponManager.HasWeapon(weaponToAcquire) == true 면 풀에서 제외
       (보유 판별은 enum type이 아닌 WeaponData 에셋 단위 — 무기별로 정확히 제외)

카드 선택 → UpgradeData.Apply(player, weaponManager)
  └── WeaponManager.AddWeapon(weaponToAcquire)
```

### 무기 로스터

| 무기 | 행동 | 데미지 판정 | 비주얼 | 구현 |
|------|------|------------|--------|------|
| 기본 투사체 | 가장 가까운 적 자동 발사 | Physics2D 트리거 | Projectile 프리팹 | 시작 무기 |
| RapidShot | 고연사 투사체 | Physics2D 트리거 | 색만 다른 프리팹 | **데이터만** (코드 0줄) |
| Bomb (AoE) | 주기 범위 폭발 | SpatialHash QueryNeighbors | LineRenderer 원 | AoEWeapon |
| Orbit | 회전 위성 접촉 | SpatialHash 틱 | 위성 프리팹 | OrbitWeapon |
| Lightning | 가까운 적 → 인근 연쇄 | SpatialHash 연쇄 탐색 | LineRenderer 절차적 볼트 | LightningWeapon |

### 설계 결정

- **SO 오염 방지** — 무기는 `Init`에서 SO 값을 런타임 필드로 **복사**한다. 업그레이드는 런타임 필드만 수정 → 디스크의 SO 에셋이 영구 변경되는 사고를 막는다.
- **스탯 업그레이드 전체 적용** — `WeaponManager`가 누적 배율(`_damageMult` 등)을 보관하고, 보유 무기 전원에 적용 + 나중 획득 무기에도 재적용한다. FireRate/Range는 무관한 무기에서 빈 가상 메서드로 no-op.
- **추상화 정당성** — `WeaponBase`/`WeaponData` 추상화는 구현체가 4종 실재하므로 정당. 무기 2종이던 초기에는 도입하지 않았고, 다중 무기가 실제로 필요해진 시점에 도입했다.
- **신규 무기 추가 비용** — 기존 행동의 변형이면 **에셋 1개**(코드 0줄), 새 행동이면 **`WeaponBase` 서브클래스 1개 + `WeaponData` 서브클래스 1개**. 두 경우 모두 `WeaponManager`/`UpgradeManager`/UI는 수정 불필요.

---

## 메타 상점 (데이터 주도)

판 사이에 유지되는 골드로 영구 강화를 구매한다. 무기 시스템과 동일 철학 — 상점 항목은 ScriptableObject로 분리되어, 에디터에서 에셋만 추가하면 상점에 자동 등장한다 (코드 수정 0줄).

### 클래스 구조

```
ShopItemData (SO)                    id, displayName, effect, costs[], valuePerTier
  effect: enum ShopEffectType { StartHp, Damage }
  Resources/Shop/ 에 에셋 배치

ShopService (static)                 로드 · 구매 · 런 시작 파생값의 단일 접근점
GameData.Purchases: List<ShopPurchase>   구매 진행도 { id, tier } — JSON 저장
```

### 골드 경제 흐름

```
적 처치 → EnemyBase.OnDead()  [State == Playing 가드]
  └▶ Managers.Game.RunGold += _goldReward   (적 타입별 1/2/3)

게임 종료 → GameManagerEx.CommitResult()
  └▶ _gameData.TotalGold += RunGold → SaveGame()   (뱅킹)

GameScene.Awake → RunGold = 0   (판 시작 리셋)
```

`RunGold`(이번 판 획득분)는 비직렬화 런타임 값, `TotalGold`(보유 골드)만 저장. 판 도중 죽어 Rewind로 되살아나도 이중 적립되지 않도록 뱅킹은 종료 시점 1회.

### 구매 흐름

```
UI_ShopPanel.Start()
  └▶ ShopService.Items (Resources.LoadAll<ShopItemData>) 순회
       └── 항목마다 UI_ShopItemCell 동적 생성 (BuildItems 하드코딩 없음)

셀 클릭 → ShopService.Buy(item)
  ├── CanBuy? (Cost >= 0 && TotalGold >= Cost)
  ├── SpendGold(Cost) → TierOf++ → SaveGame()
  └── 성공 시 RefreshAll — 골드 변동으로 전 항목 구매가능 재평가
```

### 런 시작 적용

구매 진행도를 게임 시작 시 파생값으로 환산해 적용한다. 적용점은 호출만 담당(로직은 ShopService 집중).

```
PlayerController.Start → _maxHp += ShopService.BonusHp()          StartHp 항목 tier×value 합
WeaponManager.Start   → UpgradeDamage(ShopService.DamageMult())   Damage 항목 누적 배율
```

### 설계 결정

- **하드코딩 제거** — 항목이 코드가 아닌 SO 에셋. 상점은 스탯 강화(시작HP/데미지)만 담당하고, 무기는 레벨업 풀에서 자유 획득 (상점 무기 해금은 도입했다가 단순화를 위해 제거).
- **effect 모델은 enum 분기** — abstract SO 계층 대신 `ShopEffectType` switch. 효과 종류가 적어 서브클래스 추상화는 과설계 (CLAUDE.md 준수).
- **저장 구조** — 평면 티어 필드 대신 `List<ShopPurchase>`. 항목이 늘어도 GameData 스키마 불변. JsonUtility가 `List<[Serializable]>` 직렬화 가능.
- **신규 항목 추가 비용** — 기존 effect(StartHp/Damage)면 **에셋 1개**(코드 0줄). 새 effect 종류면 enum 값 1개 + ShopService 파생/표시 분기 추가.

---

## 업그레이드 종류 (UpgradeType)

| Type | 적용 대상 | 효과 |
|------|-----------|------|
| MoveSpeed | PlayerController._speed | ×multiplier |
| FireRate | WeaponManager → 전체 무기 | ×multiplier |
| Damage | WeaponManager → 전체 무기 | ×multiplier |
| Range | WeaponManager → 전체 무기 (detect/range) | ×multiplier |
| MaxHp | PlayerController._maxHp | +amount (즉시 회복 포함) |
| AcquireWeapon | WeaponManager.AddWeapon(weaponToAcquire) | 무기 1종 획득 (보유 시 풀 제외) |

---

## UI 자기등록 패턴

모든 UI 컴포넌트는 `Start()`에서 직접 대상을 찾아 이벤트 구독.
Inspector 드래그는 **같은 프리팹/오브젝트 내부 자식**에만 사용.

```
HpBar.Start()
  └▶ FindObjectOfType<PlayerController>()
       └── OnHpChanged 구독 → Slider 업데이트

UI_UpgradePanel.Start()
  └▶ FindObjectOfType<UpgradeManager>()
       └── OnUpgradeChoiceReady 구독 → Show()
```

---

## Object Pool

```
ObjectManager._pools: Dictionary<GameObject(prefab), Queue<GameObject>>

Get(prefab)
  ├── 풀에 오브젝트 있으면 Dequeue → SetActive(true) → 반환
  └── 없으면 Instantiate → 반환

Return(obj, prefab)
  ├── SetActive(false)
  ├── SetParent(@Pool)
  └── Enqueue
```

**적용 대상:** Enemy 3종, Projectile
**이중 반납 방지:** `Projectile.ReturnToPool()`에서 `!gameObject.activeSelf` 체크
**재사용 시 리셋:** `EnemyBase._originHp` — Init 시 저장, OnDead 시 복원

---

## Spatial Hashing

```
SpatialHashGrid._grid: Dictionary<(int, int), List<EnemyBase>>
셀 크기: _cellSize = 5f

Add(enemy)       — Init 시 등록
Remove(enemy)    — OnDead 시 해제
Move(enemy, prevPos) — EnemyMover.Update() 매 프레임, 셀 변경 시만 갱신
FindNearest(origin, detectRange) — 인접 셀만 순회, sqrMagnitude로 Sqrt 생략
```

**탐색 범위:** `detectRange / cellSize` 기준으로 searchRadius 계산 → 인접 N×N 셀만 순회

---

## GPU Instancing

```
EnemyInstanceRenderer (적 타입별 1개, 씬의 @Renderers 하위)
  ├── [SerializeField] _targetPrefab  — 담당 프리팹 (registry 키)
  ├── [SerializeField] _mesh          — Quad
  ├── [SerializeField] _material      — URP Unlit, GPU Instancing ON
  └── LateUpdate()
       ├── 활성 적 순회 → Matrix4x4[] 수집
       └── Graphics.DrawMeshInstanced(_mesh, 0, _material, _matrixBuffer, count)

static _registry: Dictionary<GameObject(prefab), EnemyInstanceRenderer>
EnemyBase.Init()   → EnemyInstanceRenderer.Register(this, prefab)
EnemyBase.OnDead() → EnemyInstanceRenderer.Unregister(this, prefab)
```

**Draw Call:** 적 N마리 → 타입별 1회 (최대 1023개/배치)
**Enemy 프리팹:** SpriteRenderer 제거 — 위치 정보만 사용

---

## Job System — Separation Steering 병렬화

### 배경
EnemyMover.Update()에서 적마다 매 프레임 QueryNeighbors를 호출하는 구조가 병목.
1000마리 기준 QueryNeighbors 79ms = PlayerLoop 90ms의 88% 차지 → <10 FPS.

### 구조

```
매 프레임 (EnemyJobScheduler.Update)

  1. Gather — EnemyBase.Registry → NativeArray 복사
       positions[i]  = 적i 위치 (float2)
       entityIds[i]  = 적i EntityId
       indexOf[id]   = i  (EntityId → 배열 인덱스 역매핑)

  2. Schedule — SeparationJob.Schedule(_count, batchSize)
       워커 스레드 N개에 1000개 작업을 분산
       각 스레드: Execute(index) — 전체 positions 순회, radius 내 이웃 합산

  3. Complete — 메인 스레드에서 완료 대기

  4. EnemyMover.Update() — GetSeparation(EntityId) 조회 → 이동 방향 계산
```

### 파일 역할

| 파일 | 역할 |
|------|------|
| `SeparationJob.cs` | `[BurstCompile] IJobParallelFor` 계산 커널. 적 1개당 Execute 1회. |
| `EnemyJobScheduler.cs` | NativeArray 파이프라인 총괄. Gather → Schedule → Complete. |
| `EnemyMover.cs` | QueryNeighbors 루프 제거. GetSeparation 조회 후 이동만 처리. |

### 설계 결정

- **SpatialHashGrid 미수정** — Rewind/BombWeapon이 Dictionary 기반 grid에 의존. Job 전용 파이프라인을 별도 신설.
- **NativeArray Persistent 재사용** — 매 프레임 할당 대신 용량 부족 시만 2배 재할당. GC 압박 없음.
- **Dispose 보장** — `OnDestroy` + 재할당 직전 양쪽에서 `DisposeArrays()` 호출.
- **State 가드** — `Rewinding` 중 Job 미실행 → `HasResults = false` → EnemyMover는 separation = zero 폴백. Rewind 안전성 유지.
- **동기 완료** — `Schedule` 직후 `Complete()` 호출. 한 프레임 내 결과 확정 → Rewind 스냅샷 일관성 보장.

### 성능 결과 (Before → After)

| 적 수 | FPS Before | FPS After | QueryNeighbors |
|-------|-----------|-----------|----------------|
| 1000  | <10       | ~200      | 79ms → 0.6ms (Job+Burst) |
| 2000  | 미측정    | ~40       | — → 1.6ms (Job+Burst) |

### ProfilerMarker

| 마커 | 1000마리 | 2000마리 |
|------|---------|---------|
| `EnemyJob.Gather` | ~0.3ms | ~0.5ms |
| `EnemyJob.Schedule` | ~0.3ms | ~1.1ms |
| 합계 | ~0.6ms | ~1.6ms |

---

## Time Rewind

### Circular Buffer 구조

```
RewindManager
  ├── _buffer: FrameSnapshot[100]   — 고정 크기 배열 (5초 × 20fps)
  ├── _head: int                    — 가장 오래된 슬롯 인덱스
  ├── _tail: int                    — 다음에 쓸 슬롯 인덱스
  └── _count: int                   — 현재 저장된 슬롯 수

Record() — 0.05초마다 호출
  버퍼가 꽉 차면 _head를 한 칸 앞으로 밀어 가장 오래된 것을 덮어씀
  → 항상 최신 5초치만 유지, 메모리 고정
```

```
FrameSnapshot
  ├── PlayerSnapshot  — position, hp, exp, level, expToNextLevel, speed, maxHp
  ├── EnemySnapshot[] — entityId, enemy(ref), prefab, position, hp, speed, attackCooldown
  ├── WaveSnapshot    — gameTime, spawnTimer, waveIndex
  └── BossSnapshot    — active, entityId, sequenceIndex, actionState, stateTimer,
                        cooldownTimer, lockedPoint   (프레임당 1개. 아래 보스 시스템 참고)
```

### 되감기 실행 흐름

```
[Shift 입력 or 사망 감지]
  └▶ DoRewind() 코루틴 시작
       ├── GameState = Rewinding  (모든 Update 차단)
       ├── 버퍼 인덱스 배열 생성 [_head ... _tail-1]
       └── 역순 재생 (최신 → 과거, 0.05초 간격)
            ├── ApplyPlayer(frame.player)
            │    └── RestoreSnapshot() → OnHpChanged/OnExpChanged 이벤트 → UI 갱신
            ├── ApplyEnemies(frame.enemies)  ← 핵심 (아래 참고)
            └── ApplyWave(frame.wave)
                 └── gameTime, spawnTimer, waveIndex 복원
       └── 완료: _count=0, GameState = Playing, 쿨타임 시작
```

### 적 되감기 — EntityId 기반 3-케이스 diff

풀(Pool) 재사용으로 인해 오브젝트 참조(enemy ref)만으로는 "같은 적"인지 판별 불가.
→ Init() 시 정적 카운터로 고유 EntityId 부여. 레지스트리(`Dictionary<int, EnemyBase>`)로 활성 적 추적.

```
ApplyEnemies(snapshots)

  Step 1. 스냅샷 entityId → HashSet 구성

  Step 2. Case C — 현재 살아있지만 스냅샷에 없음 (되감기 구간 중 스폰된 적)
    Registry 순회 → snapshotIds에 없으면:
      EnemyInstanceRenderer.Unregister()
      SpatialHashGrid.Remove()
      EnemyBase.UnregisterForRewind()   ← 레지스트리에서 제거
      Managers.Object.Return()          ← 풀 반납

  Step 3. Case A / B — 스냅샷에 있는 적 복원
    ├── Case A: Registry에 entityId 존재 (현재 살아있음)
    │    SpatialHashGrid.Remove() → RestoreSnapshot() → SpatialHashGrid.Add()
    │
    └── Case B: Registry에 entityId 없음 (되감기 구간 중 죽은 적 → 부활)
         ├── B1: s.enemy.EntityId == s.entityId  (Pool 재사용 없음)
         │    SetActive(true) → RestoreSnapshot()
         │    RegisterForRewind() → InstanceRenderer.Register() → SpatialHashGrid.Add()
         │
         └── B2: s.enemy.EntityId != s.entityId  (Pool 재사용됨 — 다른 적이 같은 오브젝트 점유)
              Managers.Object.Get(prefab)           ← 풀에서 새 오브젝트
              EnemyBase.ForceRestore(s, target)     ← entityId 강제 주입 + 상태 복원
              InstanceRenderer.Register() → SpatialHashGrid.Add()
```

**ForceRestore가 필요한 이유:**
일반 `Init()`은 `_nextEntityId++`로 새 ID를 발급한다. 되감기 시에는 스냅샷의 entityId를 그대로 유지해야 이후 프레임에서 동일 적으로 인식되므로 ID를 강제 주입하는 별도 경로가 필요.

**능동 Rewind:** Shift 키, 쿨타임 30초
**Auto-Rewind:** 사망 시 1회 자동 발동, 소진 후 GameOver

---

## UI Kit — 코드기반 UGUI 폴리싱 시스템

외부 리소스 없이(절차적 생성) UGUI 화면을 일관되게 리스킨하는 3-레이어 시스템.
에디터를 드래그로 못 만지는 AI 에이전트(game-ui-artist)가 끝까지 코드로 저작하되,
산출물은 `[MenuItem]` 생성기가 만든 **편집 가능한 프리팹/씬**이 되도록 설계.

```
[레이어 3] 에디터 생성기  [MenuItem] → 씬 in-place 리스킨 + [SerializeField] 코드 배선
              │  ShopUIGenerator (Tools/UI/Build Shop Popup)
              │  MenuBackgroundGenerator (Tools/UI/Build Menu Background)
              ▼ 사용
[레이어 2] 토큰 + 컴포넌트  UITheme(색·여백·라운드·폰트 단일 출처) · UIBuild(공유 조립 헬퍼)
              ▼ 사용
[레이어 1] 절차적 스프라이트  UISpriteFactory(라운드+외곽선 9-slice, Texture2D 픽셀 생성)
```

**핵심 설계 결정:**
- **비주얼/로직 분리** — 기존 UI 로직(UI_ShopPanel/Cell, ShopService)은 무수정.
  생성기는 `SerializedObject`로 참조를 **null일 때만** 배선해 기존 연결을 보존.
- **in-place 리스킨** — 파괴 후 재생성 금지. 기존 씬 오브젝트를 찾아 컴포넌트만 갱신,
  자식 재부모화로 참조 유지. 재실행해도 이름으로 재사용(idempotent).
- **절차 스프라이트는 런타임 적용** — `new Texture2D`는 직렬화 안 되므로 프리팹/씬에
  굽지 않고, `UIProceduralSprite`(`[ExecuteAlways]`)가 `OnEnable`에서 파라미터로
  재생성해 적용. (KB: `procedural-sprite-not-serialized`)
- **하네스** — game-ui-artist 에이전트 + ui-kit 스킬. unity-dev Phase 2가 UI 요청을
  이 에이전트로 라우팅. 스크린샷 기반 반복으로 시각 폴리싱.

## 보스 시스템 (데이터 주도 + Rewind 연계)

### 클래스 구조

```
BossData (ScriptableObject)          — 정체/시퀀스/패턴 파라미터/페이즈/리쉬 단일 원본
  └── patternSequence: BossPatternType[]   { GroundSlam, RadialBurst, Charge }

BossController : EnemyBase           — 이동·리쉬·패턴 상태머신·보상. static Instance
  ├── BossTelegraph   (프리팹 내부 자식)   — 예고 링/채움 시각화
  └── BossProjectile  (풀)                — 방사탄. static _active + ClearAll()
```

`EnemyBase`를 직상속하는 것이 핵심이다. EntityId/Registry/스냅샷/피격/풀을 그대로 얻고,
`SpatialHashGrid`에도 등록되어 **기존 무기(Projectile·AoE·Lightning)가 수정 없이 보스를 맞힌다.**

### 일반 적 파이프라인과의 분리

| 시스템 | 보스 편입 | 근거 |
|--------|----------|------|
| `EnemyBase` 상속 / SpatialHash 등록 | O (필수) | 되감기·무기 판정이 전부 여기 의존 |
| `EnemyInstanceRenderer` (GPU 인스턴싱) | **X** | 1개체라 Draw Call 이득 0. Matrix만 넘기는 구조라 개체별 색(페이즈 전환)·피격 플래시 불가 → SpriteRenderer 직접 사용 |
| `SeparationJob` (Burst) | **X** | 잡몹에 밀리면 장판·돌진 시작 위치가 흔들려 되감기 재현성이 깨짐 |
| `EnemyMover` | **X** | 추격 이동이 패턴 상태머신과 충돌 |

> 인스턴싱 제외는 코드 수정 0이다 — `EnemyInstanceRenderer.Register`가 미등록 프리팹에
> no-op이라, 렌더러를 씬에 두지 않으면 자동으로 빠진다. 단 **프리팹에 SpriteRenderer를
> 직접 붙여야 한다**(잡몹 프리팹엔 인스턴싱 때문에 없다. KB: `instanced-enemy-prefab-lacks-components`)

### 패턴 상태머신

```
Idle ──(cooldownTimer 소진)──▶ Telegraph ──(stateTimer 소진)──▶ Execute ──▶ Idle
      patternSequence[i] 선택        보스 정지                  피해 판정      i = (i+1) % Length
```

- **`patternSequence.Length`에 어떤 가정도 두지 않는다.** 길이 1~N에서 동작해야
  SWARMLORD(2번째 보스)를 **C# 0줄**로 만들 수 있다 — 에셋만 복제해 시퀀스·수치를 바꾼다
- 장판은 텔레그래프 진입 순간의 플레이어 위치를 `lockedPoint`에 **고정**한다

### Rewind 연계 — 이 시스템의 핵심

되감기가 "주사위 재굴림"이 아니라 **"재도전"** 이 되도록 설계했다.

| 규칙 | 효과 |
|------|------|
| **패턴 순서 고정(랜덤 금지)** | 되감기 후 같은 패턴이 같은 타이밍에 온다 → 이번엔 피한다 |
| 보스 HP도 5초 전으로 복원 | 되감기 = 목숨 대신 5초치 딜을 잃음 → **별도 페널티 시스템 불필요** |
| 보스 등장 시 쿨타임 0 리셋 | "되감기 1장을 들고 보스전에 들어간다" 보장 |
| 되감기 시작 시 탄막 소거 | 탄을 스냅샷에 넣지 않는다(버퍼 비용·엣지케이스 회피) |

**스냅샷 배치 결정** — `BossSnapshot`을 `FrameSnapshot`에 **프레임당 1개** 둔다.
`EnemySnapshot` 확장은 1개체용 필드를 적 전원(수백 개 × 100프레임)에 붙이는 꼴이고,
별도 링버퍼는 `_head/_tail/_count`와 리셋을 두 벌 유지하다 **두 버퍼가 어긋나는 순간
"같은 타이밍" 보장이 조용히 깨진다.** `WaveSnapshot`이 이미 같은 자리(프레임당 1개 시스템
상태)를 차지하고 있어 기존 문법을 그대로 따랐다.

HP·위치·접촉쿨다운은 `BossSnapshot`에 **넣지 않는다** — 보스도 `EnemyBase.Registry`에
있으므로 기존 `EnemySnapshot` 경로가 이미 기록·복원한다(두 곳에서 같은 값을 관리하면 어긋난다).

복원 순서가 중요하다: `ApplyEnemies` → **`ApplyBoss`** → `ApplyWave`.
`ApplyEnemies`에서 보스가 풀에서 부활(Case B2)하거나 반납(Case C)될 수 있어, 그 뒤라야
`Instance`가 확정된다.

### 등장 / 보상 / 퀘스트

- **등장:** `WaveData.isBossWave` + `bossPrefab` — 기존 `startTime` 전환 파이프라인에 그대로 얹었다.
  별도 씬 없음. `BossWarningActive`는 상태 플래그가 아니라 `gameTime`에서 **매 프레임 계산하는
  프로퍼티**다 → 복원할 상태가 없으니 되감기 버그도 없다
- **중복 스폰 가드:** 되감기로 시간이 되돌아간 뒤 다시 전진하면 웨이브 전환이 재발화한다
  → `BossController.Instance == null`일 때만 스폰
- **보상:** 대량 EXP + 골드(기존 `_expReward`/`_goldReward` 경로 재사용) + **Auto-Rewind 충전 +1**
  + 되감기 쿨타임 0 + 화면 정화. 전부 `State == Playing` 게이팅 아래 → 되감기 리플레이 중 중복 지급 불가
- **퀘스트:** `QuestStat.BossKills`(enum **맨 뒤**에 추가 — 중간 삽입 시 기존 에셋의 정수값이 밀린다)
  + `GameData.LifetimeBossKills`. 기존 `RunKills`/`LifetimeKills` 패턴 복제

### 상단 HUD 밴드 소유권 — `UIHudLayout`

보스 HP 바를 추가하며 **Pause 버튼과 같은 좌표에 배치되는 버그**가 났다. 원인은 생성기마다
"StatusStrip 아래"를 각자 계산한 것 — 기준점은 공유하는데 **누가 그 슬롯을 차지했는지는
아무도 몰랐다.**

→ `UIHudLayout`이 상단 밴드 좌표(StatusStrip → 보스 HP → WARNING)를 **단일 소유**하고,
생성기는 거기서만 읽는다. 상수 복사 금지. 토글로 다른 HUD가 튀면 안 되므로 고정 슬롯이며
`VerticalLayoutGroup`을 쓰지 않는다. `Tools/UI/Check HUD Overlap`이 회귀를 검사한다.

## 향후 교체 예정 항목

| 현재 | 교체 시점 | 교체 대상 |
|------|-----------|-----------|
| Physics2D Trigger (적-플레이어 접촉) | 추후 | Spatial Hashing 기반 근접 체크 |

---

## AI 도구 공통 설정

Claude Code와 Codex를 함께 사용할 때 상세 규칙이 서로 다른 도구 설정에 중복되지 않도록
공통 원본과 도구별 어댑터를 분리한다.

```
AGENTS.md                 프로젝트 규칙 단일 원본
.agents/roles/            역할별 상세 지시 단일 원본
.agents/skills/           공통 스킬 단일 원본
.claude/knowledge/        공통 Knowledge Base 단일 원본
.claude/agents/           Claude 형식 메타데이터 어댑터
.codex/agents/            Codex TOML 메타데이터 어댑터
```

Claude가 인식하는 스킬 사본만 `.agents/Sync-ClaudeSkills.ps1`로 갱신한다. 역할과
프로젝트 규칙은 각 어댑터가 공통 원본을 직접 읽으므로 별도 동기화하지 않는다.
