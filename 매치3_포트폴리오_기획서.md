# 매치3 포트폴리오 게임 기획서
**프로젝트명:** Match & Drop (가칭)
**엔진:** Unity 2D
**개발 기간:** 3~4개월 (퇴근 후 작업 기준)
**목적:** 클라이언트 게임플레이 프로그래머 포트폴리오

---

## 1. 게임 개요

### 한 줄 설명
> 8x8 보드에서 같은 색 블록 3개 이상 매치해 제거하고, 스테이지 목표를 클리어하는 캐주얼 퍼즐 게임

### 레퍼런스
- Candy Crush Saga (기본 매치3 구조)
- Royal Match (심플한 UI/UX)

### 타겟 플랫폼
- PC (Editor 실행 기준) → 빌드는 Windows or WebGL

---

## 2. 핵심 게임플레이

### 기본 규칙
- 8x8 그리드 보드
- 인접한 블록과 스왑
- 같은 색 블록 3개 이상 연결 시 제거
- 제거 후 위에서 블록 낙하 (Gravity)
- 낙하 후 추가 매치 발생 시 콤보 체인

### 블록 종류 (MVP 기준)
| 타입 | 설명 |
|------|------|
| 기본 블록 | 5가지 색상 |
| 줄 폭탄 | 4개 매치 시 생성, 가로/세로 한 줄 제거 |
| 색상 폭탄 | 5개 매치 시 생성, 같은 색 전체 제거 |

### 스테이지 구조
- 스테이지당 목표 1~2개 (특정 블록 N개 제거, 특정 칸 클리어 등)
- 제한 이동 횟수 내 클리어
- MVP: 5스테이지

---

## 3. 기술 어필 포인트 (핵심!)

### ① 데이터 드리븐 보드/스테이지 설계 (ScriptableObject 기반)

**왜 어필되냐:**
- 기획자가 코드 수정 없이 스테이지 추가/수정 가능한 구조
- 실무에서 기획-개발 협업 방식 이해하고 있다는 증명
- 슬롯 페이테이블 경험과 연결해서 설명 가능

**구현 내용:**
```
StageData (ScriptableObject)
  ├── boardWidth, boardHeight
  ├── moveLimit
  ├── blockSpawnWeights[]     ← 블록 색상별 등장 가중치
  ├── goals[]                 ← 클리어 조건 리스트
  └── specialBlockRules[]     ← 스페셜 블록 생성 조건
```

**면접 멘트 예시:**
> "ScriptableObject로 스테이지 데이터를 분리해서, 기획 변경 시 코드를 건드리지 않아도 되는 구조로 설계했습니다. 전 직장에서 슬롯 페이테이블을 테이블 기반으로 관리하던 경험에서 착안했습니다."

---

### ② 매치 탐색 + 콤보 체인 시퀀스 설계

**왜 어필되냐:**
- 단순 BFS 구현이 아니라 "연출과 로직의 분리"를 보여줄 수 있음
- 입력 → 판정 → 연출 → 결과 파이프라인을 명확히 설계한 것 자체가 어필 포인트
- 코루틴/async 활용한 시퀀스 제어 능력 증명

**구현 내용:**
```
GameFlow 파이프라인
  1. InputPhase     : 스왑 입력 받기
  2. ValidatePhase  : 매치 가능 여부 판정 (BFS)
  3. RemovePhase    : 매치된 블록 제거
  4. AnimPhase      : 제거 연출 재생 (Object Pool에서 이펙트 꺼냄)
  5. DropPhase      : 중력 낙하 처리
  6. RefillPhase    : 빈 칸 채우기
  7. ChainCheck     : 추가 매치 발생 시 3번으로 돌아감 (콤보)
  8. InputPhase     : 다시 입력 대기
```

**핵심 설계 포인트:**
- 각 Phase를 State Machine 또는 Coroutine Chain으로 분리
- 로직(GameLogic)과 연출(BoardAnimator)을 별도 클래스로 분리
- 콤보 발생 시 연출이 끝난 후 다음 Phase 진행 보장

---

### ③ 오브젝트 풀링 (GC 스파이크 방지)

**왜 어필되냐:**
- 모바일 캐주얼 게임에서 필수 실무 지식
- "성능을 고려하며 개발한다"는 인식 심어줄 수 있음

**구현 대상:**
- 블록 GameObject
- 매치 이펙트 파티클
- 낙하 트윈 오브젝트

---

## 4. 개발 범위 (MVP vs 제외)

### MVP에 포함
- [x] 8x8 보드 생성 및 렌더링
- [x] 스왑 입력 처리
- [x] BFS 매치 탐색
- [x] 블록 제거 + 낙하 + 리필
- [x] 콤보 체인
- [x] 기본 블록 2종 (기본 + 줄 폭탄)
- [x] ScriptableObject 기반 스테이지 데이터
- [x] 스테이지 5개
- [x] 오브젝트 풀링
- [x] 간단한 UI (점수, 이동 횟수, 목표)

### 의도적으로 제외 (범위 오버 방지)
- [ ] 서버/랭킹
- [ ] 인앱결제
- [ ] 다양한 스페셜 블록 (색상 폭탄 이상)
- [ ] 튜토리얼
- [ ] 사운드 (시간 남으면 추가)

---

## 5. 개발 일정 (3~4개월, 주 3~4회 작업 기준)

| 월차 | 목표 |
|------|------|
| **1개월차** | 보드 생성, 스왑 입력, BFS 매치 탐색, 기본 블록 제거 |
| **2개월차** | 낙하/리필, 콤보 체인, 오브젝트 풀링, ScriptableObject 설계 |
| **3개월차** | 스페셜 블록(줄 폭탄), 스테이지 5개 제작, UI |
| **4개월차** | 버그 수정, 연출 다듬기, README 및 포트폴리오 문서 작성 |

> **4개월차 문서 작업이 포트폴리오에서 가장 중요함**
> 코드 구조, 설계 의도, 트레이드오프를 GitHub README에 정리

---

## 6. 폴더 구조 (권장)

```
Assets/
├── Scripts/
│   ├── Board/
│   │   ├── BoardManager.cs       ← 보드 전체 관리
│   │   ├── BoardAnimator.cs      ← 연출만 담당 (로직과 분리)
│   │   └── MatchFinder.cs        ← BFS 매치 탐색
│   ├── Block/
│   │   ├── Block.cs
│   │   └── BlockType.cs
│   ├── Stage/
│   │   ├── StageData.cs          ← ScriptableObject
│   │   └── StageManager.cs
│   ├── Pool/
│   │   └── ObjectPool.cs
│   └── UI/
│       └── GameUIController.cs
├── Data/
│   └── Stages/                   ← StageData 에셋 저장
└── Prefabs/
    ├── Blocks/
    └── Effects/
```

---

## 7. GitHub README에 꼭 넣을 것

1. **아키텍처 다이어그램** - GameFlow 파이프라인 시각화
2. **ScriptableObject 설계 이유** - 왜 이 구조를 선택했는지
3. **콤보 체인 로직 설명** - 코드 스니펫 포함
4. **오브젝트 풀링 before/after** - GC Profiler 캡처
5. **플레이 영상 GIF** - 최소 10초짜리 1개

---

## 8. 면접 예상 질문 & 답변 방향

**Q. 매치 탐색을 BFS로 구현한 이유는?**
> 보드가 8x8 고정 크기라 DFS/BFS 성능 차이가 미미하지만, BFS가 탐색 범위를 레이어별로 확인하기 쉬워 디버깅이 편리해서 선택했습니다.

**Q. ScriptableObject 말고 JSON으로 해도 되지 않나요?**
> 런타임 파싱 비용 없이 Unity 에디터에서 바로 수정 가능하고, 에셋 레퍼런스(프리팹, 스프라이트)를 직접 연결할 수 있어서 ScriptableObject를 선택했습니다. 서버에서 데이터를 받아야 하는 상황이라면 JSON이 맞다고 생각합니다.

**Q. 콤보 처리 중 입력을 어떻게 막았나요?**
> GameState enum으로 현재 Phase를 관리해서, InputPhase가 아닐 때는 입력을 무시하도록 했습니다.
